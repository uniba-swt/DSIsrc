/* Generated by CIL v. 1.7.3 */
/* print_CIL_Input is true */

typedef unsigned long size_t;
typedef signed char int8_t;
typedef int8_t rfbBool;
struct sraRegion;
struct sraRegion;
typedef struct sraRegion *sraRegionPtr;
struct _rect {
   int x1 ;
   int y1 ;
   int x2 ;
   int y2 ;
};
typedef struct _rect sraRect;
typedef struct sraRegion sraRegion;
struct sraSpan;
struct sraRectangleIterator {
   rfbBool reverseX ;
   rfbBool reverseY ;
   int ptrSize ;
   int ptrPos ;
   struct sraSpan **sPtrs ;
};
typedef struct sraRectangleIterator sraRectangleIterator;
struct sraRegion;
struct sraSpan {
   struct sraSpan *_next ;
   struct sraSpan *_prev ;
   int start ;
   int end ;
   struct sraRegion *subspan ;
};
typedef struct sraSpan sraSpan;
struct sraRegion {
   sraSpan front ;
   sraSpan back ;
};
typedef struct sraRegion sraSpanList;
extern int printf(char const   * __restrict  __format  , ...) ;
extern  __attribute__((__nothrow__)) void *( __attribute__((__leaf__)) malloc)(size_t __size )  __attribute__((__malloc__)) ;
extern  __attribute__((__nothrow__)) void *( __attribute__((__warn_unused_result__,
__leaf__)) realloc)(void *__ptr , size_t __size ) ;
extern  __attribute__((__nothrow__)) void ( __attribute__((__leaf__)) free)(void *__ptr ) ;
sraRegion *sraRgnCreate(void) ;
sraRegion *sraRgnCreateRect(int x1 , int y1 , int x2 , int y2 ) ;
sraRegion *sraRgnCreateRgn(sraRegion const   *src ) ;
void sraRgnDestroy(sraRegion *rgn ) ;
void sraRgnMakeEmpty(sraRegion *rgn ) ;
rfbBool sraRgnAnd(sraRegion *dst , sraRegion const   *src ) ;
void sraRgnOr(sraRegion *dst , sraRegion const   *src ) ;
rfbBool sraRgnSubtract(sraRegion *dst , sraRegion const   *src ) ;
void sraRgnOffset(sraRegion *dst , int dx , int dy ) ;
rfbBool sraRgnPopRect(sraRegion *rgn , sraRect *rect , unsigned long flags ) ;
unsigned long sraRgnCountRects(sraRegion const   *rgn ) ;
rfbBool sraRgnEmpty(sraRegion const   *rgn ) ;
sraRegion *sraRgnBBox(sraRegion const   *src ) ;
sraRectangleIterator *sraRgnGetIterator(sraRegion *s ) ;
sraRectangleIterator *sraRgnGetReverseIterator(sraRegion *s , rfbBool reverseX , rfbBool reverseY ) ;
rfbBool sraRgnIteratorNext(sraRectangleIterator *i , sraRect *r ) ;
void sraRgnReleaseIterator(sraRectangleIterator *i ) ;
void sraRgnPrint(sraRegion const   *rgn ) ;
rfbBool sraClipRect(int *x , int *y , int *w , int *h , int cx , int cy , int cw ,
                    int ch ) ;
rfbBool sraClipRect2(int *x , int *y , int *x2 , int *y2 , int cx , int cy , int cx2 ,
                     int cy2 ) ;
sraSpanList *sraSpanListDup(sraSpanList const   *src ) ;
void sraSpanListDestroy(sraSpanList *list ) ;
static sraSpan *sraSpanCreate(int start , int end , sraSpanList const   *subspan ) 
{ 
  sraSpan *item ;
  void *tmp ;
  struct sraSpan *tmp___0 ;

  {
  logComment("Function Entered");
  logVarInScope("formal", "subspan", "sraSpanList const   *", & subspan);
  logVarInScope("local", "item", "sraSpan *", & item);
  logVarInScope("local", "tmp", "void *", & tmp);
  logVarInScope("local", "tmp___0", "struct sraSpan *", & tmp___0);
  logBlockEntry("vnc-rfbregion.c", 37);
  tmp = malloc(sizeof(sraSpan ));
  logAssignNoOffset("vnc-rfbregion.c", 37, & tmp, "void *", "tmp", tmp, "void", "malloc",
                    1);
  logMallocInfo("sizeof(sraSpan )", sizeof(sraSpan ));
  item = (sraSpan *)tmp;
  logAssignNoOffset("vnc-rfbregion.c", 37, & item, "struct sraSpan *", "item", item,
                    "struct sraSpan", "(sraSpan *)tmp", 0);
  tmp___0 = (struct sraSpan *)((void *)0);
  logAssignNoOffset("vnc-rfbregion.c", 38, & tmp___0, "struct sraSpan *", "tmp___0",
                    tmp___0, "struct sraSpan", "(struct sraSpan *)((void *)0)", 0);
  item->_prev = tmp___0;
  logAssignOffset("vnc-rfbregion.c", 38, item, "struct sraSpan", "*item", & item->_prev,
                  "struct sraSpan *", "item->_prev", item->_prev, "struct sraSpan",
                  "tmp___0", 0);
  item->_next = tmp___0;
  logAssignOffset("vnc-rfbregion.c", 38, item, "struct sraSpan", "*item", & item->_next,
                  "struct sraSpan *", "item->_next", item->_next, "struct sraSpan",
                  "tmp___0", 0);
  item->start = start;
  item->end = end;
  logBlockExit("vnc-rfbregion.c", 41);
  item->subspan = sraSpanListDup(subspan);
  logBlockEntry("vnc-rfbregion.c", 41);
  logAssignOffset("vnc-rfbregion.c", 41, item, "struct sraSpan", "*item", & item->subspan,
                  "struct sraRegion *", "item->subspan", item->subspan, "struct sraRegion",
                  "sraSpanListDup", 0);
  dumpVarsOutOfScope();
  logBlockExit("", -1);
  {
  logVarOutOfScope("subspan", & subspan);
  logVarOutOfScope("item", & item);
  logVarOutOfScope("tmp", & tmp);
  logVarOutOfScope("tmp___0", & tmp___0);
  {
  logComment("Function Exited");
  return (item);
  }
  }
}
}
static sraSpan *sraSpanDup(sraSpan const   *src ) 
{ 
  sraSpan *span ;

  {
  logComment("Function Entered");
  logVarInScope("formal", "src", "sraSpan const   *", & src);
  logVarInScope("local", "span", "sraSpan *", & span);
  if (! src) {
    {
    logVarOutOfScope("src", & src);
    logVarOutOfScope("span", & span);
    {
    logComment("Function Exited");
    return ((sraSpan *)((void *)0));
    }
    }
  }
  span = sraSpanCreate((int )src->start, (int )src->end, (sraSpanList const   *)src->subspan);
  logBlockEntry("vnc-rfbregion.c", 49);
  logAssignNoOffset("vnc-rfbregion.c", 49, & span, "struct sraSpan *", "span", span,
                    "struct sraSpan", "sraSpanCreate", 0);
  dumpVarsOutOfScope();
  logBlockExit("", -1);
  {
  logVarOutOfScope("src", & src);
  logVarOutOfScope("span", & span);
  {
  logComment("Function Exited");
  return (span);
  }
  }
}
}
static void sraSpanInsertAfter(sraSpan *newspan , sraSpan *after ) 
{ 


  {
  logComment("Function Entered");
  logVarInScope("formal", "newspan", "sraSpan *", & newspan);
  logVarInScope("formal", "after", "sraSpan *", & after);
  logBlockEntry("vnc-rfbregion.c", 55);
  newspan->_next = after->_next;
  logAssignOffset("vnc-rfbregion.c", 55, newspan, "struct sraSpan", "*newspan", & newspan->_next,
                  "struct sraSpan *", "newspan->_next", newspan->_next, "struct sraSpan",
                  "after->_next", 0);
  newspan->_prev = after;
  logAssignOffset("vnc-rfbregion.c", 56, newspan, "struct sraSpan", "*newspan", & newspan->_prev,
                  "struct sraSpan *", "newspan->_prev", newspan->_prev, "struct sraSpan",
                  "after", 0);
  (after->_next)->_prev = newspan;
  logAssignOffset("vnc-rfbregion.c", 57, after->_next, "struct sraSpan", "*(after->_next)",
                  & (after->_next)->_prev, "struct sraSpan *", "(after->_next)->_prev",
                  (after->_next)->_prev, "struct sraSpan", "newspan", 0);
  after->_next = newspan;
  logAssignOffset("vnc-rfbregion.c", 58, after, "struct sraSpan", "*after", & after->_next,
                  "struct sraSpan *", "after->_next", after->_next, "struct sraSpan",
                  "newspan", 0);
  logBlockExit("vnc-rfbregion.c", 58);
  {
  logVarOutOfScope("newspan", & newspan);
  logVarOutOfScope("after", & after);
  {
  logComment("Function Exited");
  return;
  }
  }
}
}
static void sraSpanInsertBefore(sraSpan *newspan , sraSpan *before ) 
{ 


  {
  logComment("Function Entered");
  logVarInScope("formal", "newspan", "sraSpan *", & newspan);
  logVarInScope("formal", "before", "sraSpan *", & before);
  logBlockEntry("vnc-rfbregion.c", 63);
  newspan->_next = before;
  logAssignOffset("vnc-rfbregion.c", 63, newspan, "struct sraSpan", "*newspan", & newspan->_next,
                  "struct sraSpan *", "newspan->_next", newspan->_next, "struct sraSpan",
                  "before", 0);
  newspan->_prev = before->_prev;
  logAssignOffset("vnc-rfbregion.c", 64, newspan, "struct sraSpan", "*newspan", & newspan->_prev,
                  "struct sraSpan *", "newspan->_prev", newspan->_prev, "struct sraSpan",
                  "before->_prev", 0);
  (before->_prev)->_next = newspan;
  logAssignOffset("vnc-rfbregion.c", 65, before->_prev, "struct sraSpan", "*(before->_prev)",
                  & (before->_prev)->_next, "struct sraSpan *", "(before->_prev)->_next",
                  (before->_prev)->_next, "struct sraSpan", "newspan", 0);
  before->_prev = newspan;
  logAssignOffset("vnc-rfbregion.c", 66, before, "struct sraSpan", "*before", & before->_prev,
                  "struct sraSpan *", "before->_prev", before->_prev, "struct sraSpan",
                  "newspan", 0);
  logBlockExit("vnc-rfbregion.c", 66);
  {
  logVarOutOfScope("newspan", & newspan);
  logVarOutOfScope("before", & before);
  {
  logComment("Function Exited");
  return;
  }
  }
}
}
static void sraSpanRemove(sraSpan *span ) 
{ 


  {
  logComment("Function Entered");
  logVarInScope("formal", "span", "sraSpan *", & span);
  logBlockEntry("vnc-rfbregion.c", 71);
  (span->_prev)->_next = span->_next;
  logAssignOffset("vnc-rfbregion.c", 71, span->_prev, "struct sraSpan", "*(span->_prev)",
                  & (span->_prev)->_next, "struct sraSpan *", "(span->_prev)->_next",
                  (span->_prev)->_next, "struct sraSpan", "span->_next", 0);
  (span->_next)->_prev = span->_prev;
  logAssignOffset("vnc-rfbregion.c", 72, span->_next, "struct sraSpan", "*(span->_next)",
                  & (span->_next)->_prev, "struct sraSpan *", "(span->_next)->_prev",
                  (span->_next)->_prev, "struct sraSpan", "span->_prev", 0);
  logBlockExit("vnc-rfbregion.c", 72);
  {
  logVarOutOfScope("span", & span);
  {
  logComment("Function Exited");
  return;
  }
  }
}
}
static void sraSpanDestroy(sraSpan *span ) 
{ 


  {
  logComment("Function Entered");
  logVarInScope("formal", "span", "sraSpan *", & span);
  if (span->subspan) {
    sraSpanListDestroy(span->subspan);
    logBlockEntry("", -1);
    dumpVarsOutOfScope();
    logBlockExit("", -1);
  }
  logBlockEntry("", -1);
  logFree("vnc-rfbregion.c", 78, "(void *)span", (void *)span);
  free((void *)span);
  logBlockExit("vnc-rfbregion.c", 78);
  {
  logVarOutOfScope("span", & span);
  {
  logComment("Function Exited");
  return;
  }
  }
}
}
static void sraSpanPrint(sraSpan const   *s ) ;
static void sraSpanListPrint(sraSpanList const   *l ) 
{ 
  sraSpan *curr ;

  {
  logComment("Function Entered");
  logVarInScope("formal", "l", "sraSpanList const   *", & l);
  logVarInScope("local", "curr", "sraSpan *", & curr);
  if (! l) {
    printf((char const   * __restrict  )"NULL");
    {
    logVarOutOfScope("l", & l);
    logVarOutOfScope("curr", & curr);
    {
    logComment("Function Exited");
    return;
    }
    }
  }
  logBlockEntry("vnc-rfbregion.c", 103);
  curr = (sraSpan *)l->front._next;
  logAssignNoOffset("vnc-rfbregion.c", 103, & curr, "struct sraSpan *", "curr", curr,
                    "struct sraSpan", "(sraSpan *)l->front._next", 0);
  logBlockExit("vnc-rfbregion.c", 104);
  printf((char const   * __restrict  )"[");
  while ((unsigned long )curr != (unsigned long )(& l->back)) {
    logComment("Loop-iteration-start_vnc-rfbregion.c_105");
    {
    sraSpanPrint((sraSpan const   *)curr);
    logBlockEntry("", -1);
    dumpVarsOutOfScope();
    curr = curr->_next;
    logAssignNoOffset("vnc-rfbregion.c", 107, & curr, "struct sraSpan *", "curr",
                      curr, "struct sraSpan", "curr->_next", 0);
    logBlockExit("vnc-rfbregion.c", 107);
    }
    logComment("Loop-iteration-end");
  }
  printf((char const   * __restrict  )"]");
  {
  logVarOutOfScope("l", & l);
  logVarOutOfScope("curr", & curr);
  {
  logComment("Function Exited");
  return;
  }
  }
}
}
static void sraSpanPrint(sraSpan const   *s ) 
{ 


  {
  logComment("Function Entered");
  logVarInScope("formal", "s", "sraSpan const   *", & s);
  printf((char const   * __restrict  )"(%d-%d)", s->start, s->end);
  if (s->subspan) {
    sraSpanListPrint((sraSpanList const   *)s->subspan);
    logBlockEntry("", -1);
    dumpVarsOutOfScope();
    logBlockExit("", -1);
  }
  {
  logVarOutOfScope("s", & s);
  {
  logComment("Function Exited");
  return;
  }
  }
}
}
static sraSpanList *sraSpanListCreate(void) 
{ 
  sraSpanList *item ;
  void *tmp ;

  {
  logComment("Function Entered");
  logVarInScope("local", "item", "sraSpanList *", & item);
  logVarInScope("local", "tmp", "void *", & tmp);
  logBlockEntry("vnc-rfbregion.c", 121);
  tmp = malloc(sizeof(sraSpanList ));
  logAssignNoOffset("vnc-rfbregion.c", 121, & tmp, "void *", "tmp", tmp, "void", "malloc",
                    1);
  logMallocInfo("sizeof(sraSpanList )", sizeof(sraSpanList ));
  item = (sraSpanList *)tmp;
  logAssignNoOffset("vnc-rfbregion.c", 121, & item, "struct sraRegion *", "item",
                    item, "struct sraRegion", "(sraSpanList *)tmp", 0);
  item->front._next = & item->back;
  logAssignOffset("vnc-rfbregion.c", 122, item, "struct sraRegion", "*item", & item->front._next,
                  "struct sraSpan *", "item->front._next", item->front._next, "struct sraSpan",
                  "& item->back", 0);
  item->front._prev = (struct sraSpan *)((void *)0);
  logAssignOffset("vnc-rfbregion.c", 123, item, "struct sraRegion", "*item", & item->front._prev,
                  "struct sraSpan *", "item->front._prev", item->front._prev, "struct sraSpan",
                  "(struct sraSpan *)((void *)0)", 0);
  item->back._prev = & item->front;
  logAssignOffset("vnc-rfbregion.c", 124, item, "struct sraRegion", "*item", & item->back._prev,
                  "struct sraSpan *", "item->back._prev", item->back._prev, "struct sraSpan",
                  "& item->front", 0);
  item->back._next = (struct sraSpan *)((void *)0);
  logAssignOffset("vnc-rfbregion.c", 125, item, "struct sraRegion", "*item", & item->back._next,
                  "struct sraSpan *", "item->back._next", item->back._next, "struct sraSpan",
                  "(struct sraSpan *)((void *)0)", 0);
  logBlockExit("vnc-rfbregion.c", 125);
  {
  logVarOutOfScope("item", & item);
  logVarOutOfScope("tmp", & tmp);
  {
  logComment("Function Exited");
  return (item);
  }
  }
}
}
sraSpanList *sraSpanListDup(sraSpanList const   *src ) 
{ 
  sraSpanList *newlist ;
  sraSpan *newspan ;
  sraSpan *curr ;

  {
  logComment("Function Entered");
  logVarInScope("formal", "src", "sraSpanList const   *", & src);
  logVarInScope("local", "newlist", "sraSpanList *", & newlist);
  logVarInScope("local", "newspan", "sraSpan *", & newspan);
  logVarInScope("local", "curr", "sraSpan *", & curr);
  if (! src) {
    {
    logVarOutOfScope("src", & src);
    logVarOutOfScope("newlist", & newlist);
    logVarOutOfScope("newspan", & newspan);
    logVarOutOfScope("curr", & curr);
    {
    logComment("Function Exited");
    return ((sraSpanList *)((void *)0));
    }
    }
  }
  newlist = sraSpanListCreate();
  logBlockEntry("vnc-rfbregion.c", 135);
  logAssignNoOffset("vnc-rfbregion.c", 135, & newlist, "struct sraRegion *", "newlist",
                    newlist, "struct sraRegion", "sraSpanListCreate", 0);
  dumpVarsOutOfScope();
  curr = (sraSpan *)src->front._next;
  logAssignNoOffset("vnc-rfbregion.c", 136, & curr, "struct sraSpan *", "curr", curr,
                    "struct sraSpan", "(sraSpan *)src->front._next", 0);
  logBlockExit("vnc-rfbregion.c", 136);
  while ((unsigned long )curr != (unsigned long )(& src->back)) {
    logComment("Loop-iteration-start_vnc-rfbregion.c_137");
    {
    newspan = sraSpanDup((sraSpan const   *)curr);
    logBlockEntry("vnc-rfbregion.c", 138);
    logAssignNoOffset("vnc-rfbregion.c", 138, & newspan, "struct sraSpan *", "newspan",
                      newspan, "struct sraSpan", "sraSpanDup", 0);
    dumpVarsOutOfScope();
    logBlockExit("vnc-rfbregion.c", 139);
    sraSpanInsertBefore(newspan, & newlist->back);
    logBlockEntry("", -1);
    dumpVarsOutOfScope();
    curr = curr->_next;
    logAssignNoOffset("vnc-rfbregion.c", 140, & curr, "struct sraSpan *", "curr",
                      curr, "struct sraSpan", "curr->_next", 0);
    logBlockExit("vnc-rfbregion.c", 140);
    }
    logComment("Loop-iteration-end");
  }
  {
  logVarOutOfScope("src", & src);
  logVarOutOfScope("newlist", & newlist);
  logVarOutOfScope("newspan", & newspan);
  logVarOutOfScope("curr", & curr);
  {
  logComment("Function Exited");
  return (newlist);
  }
  }
}
}
void sraSpanListDestroy(sraSpanList *list ) 
{ 
  sraSpan *curr ;
  sraSpan *next ;

  {
  logComment("Function Entered");
  logVarInScope("formal", "list", "sraSpanList *", & list);
  logVarInScope("local", "curr", "sraSpan *", & curr);
  logVarInScope("local", "next", "sraSpan *", & next);
  while ((unsigned long )list->front._next != (unsigned long )(& list->back)) {
    logComment("Loop-iteration-start_vnc-rfbregion.c_149");
    {
    logBlockEntry("vnc-rfbregion.c", 150);
    curr = list->front._next;
    logAssignNoOffset("vnc-rfbregion.c", 150, & curr, "struct sraSpan *", "curr",
                      curr, "struct sraSpan", "list->front._next", 0);
    next = curr->_next;
    logAssignNoOffset("vnc-rfbregion.c", 151, & next, "struct sraSpan *", "next",
                      next, "struct sraSpan", "curr->_next", 0);
    logBlockExit("vnc-rfbregion.c", 152);
    sraSpanRemove(curr);
    logBlockEntry("", -1);
    dumpVarsOutOfScope();
    logBlockExit("vnc-rfbregion.c", 153);
    sraSpanDestroy(curr);
    logBlockEntry("", -1);
    dumpVarsOutOfScope();
    curr = next;
    logAssignNoOffset("vnc-rfbregion.c", 154, & curr, "struct sraSpan *", "curr",
                      curr, "struct sraSpan", "next", 0);
    logBlockExit("vnc-rfbregion.c", 154);
    }
    logComment("Loop-iteration-end");
  }
  logBlockEntry("", -1);
  logFree("vnc-rfbregion.c", 156, "(void *)list", (void *)list);
  free((void *)list);
  logBlockExit("vnc-rfbregion.c", 156);
  {
  logVarOutOfScope("list", & list);
  logVarOutOfScope("curr", & curr);
  logVarOutOfScope("next", & next);
  {
  logComment("Function Exited");
  return;
  }
  }
}
}
static void sraSpanListMakeEmpty(sraSpanList *list ) 
{ 
  sraSpan *curr ;
  sraSpan *next ;

  {
  logComment("Function Entered");
  logVarInScope("formal", "list", "sraSpanList *", & list);
  logVarInScope("local", "curr", "sraSpan *", & curr);
  logVarInScope("local", "next", "sraSpan *", & next);
  while ((unsigned long )list->front._next != (unsigned long )(& list->back)) {
    logComment("Loop-iteration-start_vnc-rfbregion.c_162");
    {
    logBlockEntry("vnc-rfbregion.c", 163);
    curr = list->front._next;
    logAssignNoOffset("vnc-rfbregion.c", 163, & curr, "struct sraSpan *", "curr",
                      curr, "struct sraSpan", "list->front._next", 0);
    next = curr->_next;
    logAssignNoOffset("vnc-rfbregion.c", 164, & next, "struct sraSpan *", "next",
                      next, "struct sraSpan", "curr->_next", 0);
    logBlockExit("vnc-rfbregion.c", 165);
    sraSpanRemove(curr);
    logBlockEntry("", -1);
    dumpVarsOutOfScope();
    logBlockExit("vnc-rfbregion.c", 166);
    sraSpanDestroy(curr);
    logBlockEntry("", -1);
    dumpVarsOutOfScope();
    curr = next;
    logAssignNoOffset("vnc-rfbregion.c", 167, & curr, "struct sraSpan *", "curr",
                      curr, "struct sraSpan", "next", 0);
    logBlockExit("vnc-rfbregion.c", 167);
    }
    logComment("Loop-iteration-end");
  }
  logBlockEntry("vnc-rfbregion.c", 169);
  list->front._next = & list->back;
  logAssignOffset("vnc-rfbregion.c", 169, list, "struct sraRegion", "*list", & list->front._next,
                  "struct sraSpan *", "list->front._next", list->front._next, "struct sraSpan",
                  "& list->back", 0);
  list->front._prev = (struct sraSpan *)((void *)0);
  logAssignOffset("vnc-rfbregion.c", 170, list, "struct sraRegion", "*list", & list->front._prev,
                  "struct sraSpan *", "list->front._prev", list->front._prev, "struct sraSpan",
                  "(struct sraSpan *)((void *)0)", 0);
  list->back._prev = & list->front;
  logAssignOffset("vnc-rfbregion.c", 171, list, "struct sraRegion", "*list", & list->back._prev,
                  "struct sraSpan *", "list->back._prev", list->back._prev, "struct sraSpan",
                  "& list->front", 0);
  list->back._next = (struct sraSpan *)((void *)0);
  logAssignOffset("vnc-rfbregion.c", 172, list, "struct sraRegion", "*list", & list->back._next,
                  "struct sraSpan *", "list->back._next", list->back._next, "struct sraSpan",
                  "(struct sraSpan *)((void *)0)", 0);
  logBlockExit("vnc-rfbregion.c", 172);
  {
  logVarOutOfScope("list", & list);
  logVarOutOfScope("curr", & curr);
  logVarOutOfScope("next", & next);
  {
  logComment("Function Exited");
  return;
  }
  }
}
}
static rfbBool sraSpanListEqual(sraSpanList const   *s1 , sraSpanList const   *s2 ) 
{ 
  sraSpan *sp1 ;
  sraSpan *sp2 ;
  rfbBool tmp ;

  {
  logComment("Function Entered");
  logVarInScope("formal", "s1", "sraSpanList const   *", & s1);
  logVarInScope("formal", "s2", "sraSpanList const   *", & s2);
  logVarInScope("local", "sp1", "sraSpan *", & sp1);
  logVarInScope("local", "sp2", "sraSpan *", & sp2);
  if (! s1) {
    if (! s2) {
      {
      logVarOutOfScope("s1", & s1);
      logVarOutOfScope("s2", & s2);
      logVarOutOfScope("sp1", & sp1);
      logVarOutOfScope("sp2", & sp2);
      {
      logComment("Function Exited");
      return ((rfbBool )1);
      }
      }
    } else {
      printf((char const   * __restrict  )"sraSpanListEqual:incompatible spans (only one NULL!)\n");
      {
      logVarOutOfScope("s1", & s1);
      logVarOutOfScope("s2", & s2);
      logVarOutOfScope("sp1", & sp1);
      logVarOutOfScope("sp2", & sp2);
      {
      logComment("Function Exited");
      return ((rfbBool )0);
      }
      }
    }
  }
  logBlockEntry("vnc-rfbregion.c", 188);
  sp1 = (sraSpan *)s1->front._next;
  logAssignNoOffset("vnc-rfbregion.c", 188, & sp1, "struct sraSpan *", "sp1", sp1,
                    "struct sraSpan", "(sraSpan *)s1->front._next", 0);
  sp2 = (sraSpan *)s2->front._next;
  logAssignNoOffset("vnc-rfbregion.c", 189, & sp2, "struct sraSpan *", "sp2", sp2,
                    "struct sraSpan", "(sraSpan *)s2->front._next", 0);
  logBlockExit("vnc-rfbregion.c", 189);
  while (1) {
    if ((unsigned long )sp1 != (unsigned long )(& s1->back)) {
      if (! ((unsigned long )sp2 != (unsigned long )(& s2->back))) {
        break;
      }
    } else {
      break;
    }
    logComment("Loop-iteration-start_vnc-rfbregion.c_190");
    {
    if (sp1->start != sp2->start) {
      {
      logVarOutOfScope("s1", & s1);
      logVarOutOfScope("s2", & s2);
      logVarOutOfScope("sp1", & sp1);
      logVarOutOfScope("sp2", & sp2);
      {
      logComment("Function Exited");
      return ((rfbBool )0);
      }
      }
    } else
    if (sp1->end != sp2->end) {
      {
      logVarOutOfScope("s1", & s1);
      logVarOutOfScope("s2", & s2);
      logVarOutOfScope("sp1", & sp1);
      logVarOutOfScope("sp2", & sp2);
      {
      logComment("Function Exited");
      return ((rfbBool )0);
      }
      }
    } else {
      tmp = sraSpanListEqual((sraSpanList const   *)sp1->subspan, (sraSpanList const   *)sp2->subspan);
      logBlockEntry("", -1);
      dumpVarsOutOfScope();
      logBlockExit("", -1);
      if (! tmp) {
        {
        logVarOutOfScope("s1", & s1);
        logVarOutOfScope("s2", & s2);
        logVarOutOfScope("sp1", & sp1);
        logVarOutOfScope("sp2", & sp2);
        {
        logComment("Function Exited");
        return ((rfbBool )0);
        }
        }
      }
    }
    logBlockEntry("vnc-rfbregion.c", 197);
    sp1 = sp1->_next;
    logAssignNoOffset("vnc-rfbregion.c", 197, & sp1, "struct sraSpan *", "sp1", sp1,
                      "struct sraSpan", "sp1->_next", 0);
    sp2 = sp2->_next;
    logAssignNoOffset("vnc-rfbregion.c", 198, & sp2, "struct sraSpan *", "sp2", sp2,
                      "struct sraSpan", "sp2->_next", 0);
    logBlockExit("vnc-rfbregion.c", 198);
    }
    logComment("Loop-iteration-end");
  }
  if ((unsigned long )sp1 == (unsigned long )(& s1->back)) {
    if ((unsigned long )sp2 == (unsigned long )(& s2->back)) {
      {
      logVarOutOfScope("s1", & s1);
      logVarOutOfScope("s2", & s2);
      logVarOutOfScope("sp1", & sp1);
      logVarOutOfScope("sp2", & sp2);
      {
      logComment("Function Exited");
      return ((rfbBool )1);
      }
      }
    } else {
      {
      logVarOutOfScope("s1", & s1);
      logVarOutOfScope("s2", & s2);
      logVarOutOfScope("sp1", & sp1);
      logVarOutOfScope("sp2", & sp2);
      {
      logComment("Function Exited");
      return ((rfbBool )0);
      }
      }
    }
  } else {
    {
    logVarOutOfScope("s1", & s1);
    logVarOutOfScope("s2", & s2);
    logVarOutOfScope("sp1", & sp1);
    logVarOutOfScope("sp2", & sp2);
    {
    logComment("Function Exited");
    return ((rfbBool )0);
    }
    }
  }
}
}
static rfbBool sraSpanListEmpty(sraSpanList const   *list ) 
{ 


  {
  logComment("Function Entered");
  logVarInScope("formal", "list", "sraSpanList const   *", & list);
  {
  logVarOutOfScope("list", & list);
  {
  logComment("Function Exited");
  return ((rfbBool )((unsigned long )list->front._next == (unsigned long )(& list->back)));
  }
  }
}
}
static unsigned long sraSpanListCount(sraSpanList const   *list ) 
{ 
  sraSpan *curr ;
  unsigned long count ;
  unsigned long tmp ;

  {
  logComment("Function Entered");
  logVarInScope("formal", "list", "sraSpanList const   *", & list);
  logVarInScope("local", "curr", "sraSpan *", & curr);
  logBlockEntry("vnc-rfbregion.c", 215);
  curr = (sraSpan *)list->front._next;
  logAssignNoOffset("vnc-rfbregion.c", 215, & curr, "struct sraSpan *", "curr", curr,
                    "struct sraSpan", "(sraSpan *)list->front._next", 0);
  count = 0UL;
  logBlockExit("vnc-rfbregion.c", 216);
  while ((unsigned long )curr != (unsigned long )(& list->back)) {
    logComment("Loop-iteration-start_vnc-rfbregion.c_217");
    {
    if (curr->subspan) {
      tmp = sraSpanListCount((sraSpanList const   *)curr->subspan);
      logBlockEntry("", -1);
      dumpVarsOutOfScope();
      count += tmp;
      logBlockExit("vnc-rfbregion.c", 219);
    } else {
      count ++;
    }
    logBlockEntry("vnc-rfbregion.c", 223);
    curr = curr->_next;
    logAssignNoOffset("vnc-rfbregion.c", 223, & curr, "struct sraSpan *", "curr",
                      curr, "struct sraSpan", "curr->_next", 0);
    logBlockExit("vnc-rfbregion.c", 223);
    }
    logComment("Loop-iteration-end");
  }
  {
  logVarOutOfScope("list", & list);
  logVarOutOfScope("curr", & curr);
  {
  logComment("Function Exited");
  return (count);
  }
  }
}
}
static void sraSpanMergePrevious(sraSpan *dest ) 
{ 
  sraSpan *prev ;
  rfbBool tmp ;

  {
  logComment("Function Entered");
  logVarInScope("formal", "dest", "sraSpan *", & dest);
  logVarInScope("local", "prev", "sraSpan *", & prev);
  logBlockEntry("vnc-rfbregion.c", 230);
  prev = dest->_prev;
  logAssignNoOffset("vnc-rfbregion.c", 230, & prev, "struct sraSpan *", "prev", prev,
                    "struct sraSpan", "dest->_prev", 0);
  logBlockExit("vnc-rfbregion.c", 230);
  while (1) {
    if (prev->_prev) {
      if (prev->end == dest->start) {
        tmp = sraSpanListEqual((sraSpanList const   *)prev->subspan, (sraSpanList const   *)dest->subspan);
        logBlockEntry("", -1);
        dumpVarsOutOfScope();
        logBlockExit("", -1);
        if (! tmp) {
          break;
        }
      } else {
        break;
      }
    } else {
      break;
    }
    logComment("Loop-iteration-start_vnc-rfbregion.c_232");
    {
    logBlockEntry("vnc-rfbregion.c", 242);
    dest->start = prev->start;
    logBlockExit("vnc-rfbregion.c", 243);
    sraSpanRemove(prev);
    logBlockEntry("", -1);
    dumpVarsOutOfScope();
    logBlockExit("vnc-rfbregion.c", 244);
    sraSpanDestroy(prev);
    logBlockEntry("", -1);
    dumpVarsOutOfScope();
    prev = dest->_prev;
    logAssignNoOffset("vnc-rfbregion.c", 245, & prev, "struct sraSpan *", "prev",
                      prev, "struct sraSpan", "dest->_prev", 0);
    logBlockExit("vnc-rfbregion.c", 245);
    }
    logComment("Loop-iteration-end");
  }
  {
  logVarOutOfScope("dest", & dest);
  logVarOutOfScope("prev", & prev);
  {
  logComment("Function Exited");
  return;
  }
  }
}
}
static void sraSpanMergeNext(sraSpan *dest ) 
{ 
  sraSpan *next ;
  rfbBool tmp ;

  {
  logComment("Function Entered");
  logVarInScope("formal", "dest", "sraSpan *", & dest);
  logVarInScope("local", "next", "sraSpan *", & next);
  logBlockEntry("vnc-rfbregion.c", 251);
  next = dest->_next;
  logAssignNoOffset("vnc-rfbregion.c", 251, & next, "struct sraSpan *", "next", next,
                    "struct sraSpan", "dest->_next", 0);
  logBlockExit("vnc-rfbregion.c", 251);
  while (1) {
    if (next->_next) {
      if (next->start == dest->end) {
        tmp = sraSpanListEqual((sraSpanList const   *)next->subspan, (sraSpanList const   *)dest->subspan);
        logBlockEntry("", -1);
        dumpVarsOutOfScope();
        logBlockExit("", -1);
        if (! tmp) {
          break;
        }
      } else {
        break;
      }
    } else {
      break;
    }
    logComment("Loop-iteration-start_vnc-rfbregion.c_252");
    {
    logBlockEntry("vnc-rfbregion.c", 262);
    dest->end = next->end;
    logBlockExit("vnc-rfbregion.c", 263);
    sraSpanRemove(next);
    logBlockEntry("", -1);
    dumpVarsOutOfScope();
    logBlockExit("vnc-rfbregion.c", 264);
    sraSpanDestroy(next);
    logBlockEntry("", -1);
    dumpVarsOutOfScope();
    next = dest->_next;
    logAssignNoOffset("vnc-rfbregion.c", 265, & next, "struct sraSpan *", "next",
                      next, "struct sraSpan", "dest->_next", 0);
    logBlockExit("vnc-rfbregion.c", 265);
    }
    logComment("Loop-iteration-end");
  }
  {
  logVarOutOfScope("dest", & dest);
  logVarOutOfScope("next", & next);
  {
  logComment("Function Exited");
  return;
  }
  }
}
}
static void sraSpanListOr(sraSpanList *dest , sraSpanList const   *src ) 
{ 
  sraSpan *d_curr ;
  sraSpan *s_curr ;
  int s_start ;
  int s_end ;
  sraSpan *tmp ;
  sraSpan *tmp___0 ;
  sraSpan *tmp___1 ;
  sraSpan *tmp___2 ;

  {
  logComment("Function Entered");
  logVarInScope("formal", "dest", "sraSpanList *", & dest);
  logVarInScope("formal", "src", "sraSpanList const   *", & src);
  logVarInScope("local", "d_curr", "sraSpan *", & d_curr);
  logVarInScope("local", "s_curr", "sraSpan *", & s_curr);
  logVarInScope("local", "tmp", "sraSpan *", & tmp);
  logVarInScope("local", "tmp___0", "sraSpan *", & tmp___0);
  logVarInScope("local", "tmp___1", "sraSpan *", & tmp___1);
  logVarInScope("local", "tmp___2", "sraSpan *", & tmp___2);
  if (! dest) {
    if (! src) {
      {
      logVarOutOfScope("dest", & dest);
      logVarOutOfScope("src", & src);
      logVarOutOfScope("d_curr", & d_curr);
      logVarOutOfScope("s_curr", & s_curr);
      logVarOutOfScope("tmp", & tmp);
      logVarOutOfScope("tmp___0", & tmp___0);
      logVarOutOfScope("tmp___1", & tmp___1);
      logVarOutOfScope("tmp___2", & tmp___2);
      {
      logComment("Function Exited");
      return;
      }
      }
    } else {
      printf((char const   * __restrict  )"sraSpanListOr:incompatible spans (only one NULL!)\n");
      {
      logVarOutOfScope("dest", & dest);
      logVarOutOfScope("src", & src);
      logVarOutOfScope("d_curr", & d_curr);
      logVarOutOfScope("s_curr", & s_curr);
      logVarOutOfScope("tmp", & tmp);
      logVarOutOfScope("tmp___0", & tmp___0);
      logVarOutOfScope("tmp___1", & tmp___1);
      logVarOutOfScope("tmp___2", & tmp___2);
      {
      logComment("Function Exited");
      return;
      }
      }
    }
  }
  logBlockEntry("vnc-rfbregion.c", 283);
  d_curr = dest->front._next;
  logAssignNoOffset("vnc-rfbregion.c", 283, & d_curr, "struct sraSpan *", "d_curr",
                    d_curr, "struct sraSpan", "dest->front._next", 0);
  s_curr = (sraSpan *)src->front._next;
  logAssignNoOffset("vnc-rfbregion.c", 284, & s_curr, "struct sraSpan *", "s_curr",
                    s_curr, "struct sraSpan", "(sraSpan *)src->front._next", 0);
  s_start = s_curr->start;
  s_end = s_curr->end;
  logBlockExit("vnc-rfbregion.c", 286);
  while ((unsigned long )s_curr != (unsigned long )(& src->back)) {
    logComment("Loop-iteration-start_vnc-rfbregion.c_287");
    {
    if ((unsigned long )d_curr == (unsigned long )(& dest->back)) {
      goto _L;
    } else
    if (d_curr->start >= s_end) {
      _L: /* CIL Label */ 
      tmp = sraSpanCreate(s_start, s_end, (sraSpanList const   *)s_curr->subspan);
      logBlockEntry("vnc-rfbregion.c", 294);
      logAssignNoOffset("vnc-rfbregion.c", 294, & tmp, "struct sraSpan *", "tmp",
                        tmp, "struct sraSpan", "sraSpanCreate", 0);
      dumpVarsOutOfScope();
      logBlockExit("vnc-rfbregion.c", 294);
      sraSpanInsertBefore(tmp, d_curr);
      logBlockEntry("", -1);
      dumpVarsOutOfScope();
      logBlockExit("", -1);
      if ((unsigned long )d_curr != (unsigned long )(& dest->back)) {
        sraSpanMergePrevious(d_curr);
        logBlockEntry("", -1);
        dumpVarsOutOfScope();
        logBlockExit("", -1);
      }
      logBlockEntry("vnc-rfbregion.c", 299);
      s_curr = s_curr->_next;
      logAssignNoOffset("vnc-rfbregion.c", 299, & s_curr, "struct sraSpan *", "s_curr",
                        s_curr, "struct sraSpan", "s_curr->_next", 0);
      s_start = s_curr->start;
      s_end = s_curr->end;
      logBlockExit("vnc-rfbregion.c", 301);
    } else
    if (s_start < d_curr->end) {
      if (s_end > d_curr->start) {
        if (s_start < d_curr->start) {
          tmp___0 = sraSpanCreate(s_start, d_curr->start, (sraSpanList const   *)s_curr->subspan);
          logBlockEntry("vnc-rfbregion.c", 310);
          logAssignNoOffset("vnc-rfbregion.c", 310, & tmp___0, "struct sraSpan *",
                            "tmp___0", tmp___0, "struct sraSpan", "sraSpanCreate",
                            0);
          dumpVarsOutOfScope();
          logBlockExit("vnc-rfbregion.c", 310);
          sraSpanInsertBefore(tmp___0, d_curr);
          logBlockEntry("", -1);
          dumpVarsOutOfScope();
          logBlockExit("vnc-rfbregion.c", 314);
          sraSpanMergePrevious(d_curr);
          logBlockEntry("", -1);
          dumpVarsOutOfScope();
          logBlockExit("", -1);
        }
        if (s_end < d_curr->end) {
          tmp___1 = sraSpanCreate(s_end, d_curr->end, (sraSpanList const   *)d_curr->subspan);
          logBlockEntry("vnc-rfbregion.c", 319);
          logAssignNoOffset("vnc-rfbregion.c", 319, & tmp___1, "struct sraSpan *",
                            "tmp___1", tmp___1, "struct sraSpan", "sraSpanCreate",
                            0);
          dumpVarsOutOfScope();
          logBlockExit("vnc-rfbregion.c", 319);
          sraSpanInsertAfter(tmp___1, d_curr);
          logBlockEntry("", -1);
          dumpVarsOutOfScope();
          d_curr->end = s_end;
          logBlockExit("vnc-rfbregion.c", 323);
        }
        if (s_start > d_curr->start) {
          tmp___2 = sraSpanCreate(d_curr->start, s_start, (sraSpanList const   *)d_curr->subspan);
          logBlockEntry("vnc-rfbregion.c", 326);
          logAssignNoOffset("vnc-rfbregion.c", 326, & tmp___2, "struct sraSpan *",
                            "tmp___2", tmp___2, "struct sraSpan", "sraSpanCreate",
                            0);
          dumpVarsOutOfScope();
          logBlockExit("vnc-rfbregion.c", 326);
          sraSpanInsertBefore(tmp___2, d_curr);
          logBlockEntry("", -1);
          dumpVarsOutOfScope();
          d_curr->start = s_start;
          logBlockExit("vnc-rfbregion.c", 330);
        }
        sraSpanListOr(d_curr->subspan, (sraSpanList const   *)s_curr->subspan);
        logBlockEntry("", -1);
        dumpVarsOutOfScope();
        logBlockExit("", -1);
        if ((unsigned long )d_curr->_prev != (unsigned long )(& dest->front)) {
          sraSpanMergePrevious(d_curr);
          logBlockEntry("", -1);
          dumpVarsOutOfScope();
          logBlockExit("", -1);
        }
        if ((unsigned long )d_curr->_next != (unsigned long )(& dest->back)) {
          sraSpanMergeNext(d_curr);
          logBlockEntry("", -1);
          dumpVarsOutOfScope();
          logBlockExit("", -1);
        }
        if (s_end > d_curr->end) {
          logBlockEntry("vnc-rfbregion.c", 344);
          s_start = d_curr->end;
          d_curr = d_curr->_next;
          logAssignNoOffset("vnc-rfbregion.c", 345, & d_curr, "struct sraSpan *",
                            "d_curr", d_curr, "struct sraSpan", "d_curr->_next", 0);
          logBlockExit("vnc-rfbregion.c", 345);
        } else {
          logBlockEntry("vnc-rfbregion.c", 347);
          s_curr = s_curr->_next;
          logAssignNoOffset("vnc-rfbregion.c", 347, & s_curr, "struct sraSpan *",
                            "s_curr", s_curr, "struct sraSpan", "s_curr->_next", 0);
          s_start = s_curr->start;
          s_end = s_curr->end;
          logBlockExit("vnc-rfbregion.c", 349);
        }
      } else {
        logBlockEntry("vnc-rfbregion.c", 353);
        d_curr = d_curr->_next;
        logAssignNoOffset("vnc-rfbregion.c", 353, & d_curr, "struct sraSpan *", "d_curr",
                          d_curr, "struct sraSpan", "d_curr->_next", 0);
        logBlockExit("vnc-rfbregion.c", 353);
      }
    } else {
      logBlockEntry("vnc-rfbregion.c", 353);
      d_curr = d_curr->_next;
      logAssignNoOffset("vnc-rfbregion.c", 353, & d_curr, "struct sraSpan *", "d_curr",
                        d_curr, "struct sraSpan", "d_curr->_next", 0);
      logBlockExit("vnc-rfbregion.c", 353);
    }
    }
    logComment("Loop-iteration-end");
  }
  {
  logVarOutOfScope("dest", & dest);
  logVarOutOfScope("src", & src);
  logVarOutOfScope("d_curr", & d_curr);
  logVarOutOfScope("s_curr", & s_curr);
  logVarOutOfScope("tmp", & tmp);
  logVarOutOfScope("tmp___0", & tmp___0);
  logVarOutOfScope("tmp___1", & tmp___1);
  logVarOutOfScope("tmp___2", & tmp___2);
  {
  logComment("Function Exited");
  return;
  }
  }
}
}
static rfbBool sraSpanListAnd(sraSpanList *dest , sraSpanList const   *src ) 
{ 
  sraSpan *d_curr ;
  sraSpan *s_curr ;
  sraSpan *d_next ;
  sraSpan *next ;
  sraSpan *tmp ;
  sraSpan *next___0 ;
  rfbBool tmp___0 ;
  sraSpan *next___1 ;
  rfbBool tmp___1 ;
  int tmp___2 ;

  {
  logComment("Function Entered");
  logVarInScope("formal", "dest", "sraSpanList *", & dest);
  logVarInScope("formal", "src", "sraSpanList const   *", & src);
  logVarInScope("local", "d_curr", "sraSpan *", & d_curr);
  logVarInScope("local", "s_curr", "sraSpan *", & s_curr);
  logVarInScope("local", "d_next", "sraSpan *", & d_next);
  logVarInScope("local", "next", "sraSpan *", & next);
  logVarInScope("local", "tmp", "sraSpan *", & tmp);
  logVarInScope("local", "next___0", "sraSpan *", & next___0);
  logVarInScope("local", "next___1", "sraSpan *", & next___1);
  if (! dest) {
    if (! src) {
      {
      logVarOutOfScope("dest", & dest);
      logVarOutOfScope("src", & src);
      logVarOutOfScope("d_curr", & d_curr);
      logVarOutOfScope("s_curr", & s_curr);
      logVarOutOfScope("d_next", & d_next);
      logVarOutOfScope("next", & next);
      logVarOutOfScope("tmp", & tmp);
      logVarOutOfScope("next___0", & next___0);
      logVarOutOfScope("next___1", & next___1);
      {
      logComment("Function Exited");
      return ((rfbBool )1);
      }
      }
    } else {
      printf((char const   * __restrict  )"sraSpanListAnd:incompatible spans (only one NULL!)\n");
      {
      logVarOutOfScope("dest", & dest);
      logVarOutOfScope("src", & src);
      logVarOutOfScope("d_curr", & d_curr);
      logVarOutOfScope("s_curr", & s_curr);
      logVarOutOfScope("d_next", & d_next);
      logVarOutOfScope("next", & next);
      logVarOutOfScope("tmp", & tmp);
      logVarOutOfScope("next___0", & next___0);
      logVarOutOfScope("next___1", & next___1);
      {
      logComment("Function Exited");
      return ((rfbBool )0);
      }
      }
    }
  }
  logBlockEntry("vnc-rfbregion.c", 372);
  d_curr = dest->front._next;
  logAssignNoOffset("vnc-rfbregion.c", 372, & d_curr, "struct sraSpan *", "d_curr",
                    d_curr, "struct sraSpan", "dest->front._next", 0);
  s_curr = (sraSpan *)src->front._next;
  logAssignNoOffset("vnc-rfbregion.c", 373, & s_curr, "struct sraSpan *", "s_curr",
                    s_curr, "struct sraSpan", "(sraSpan *)src->front._next", 0);
  logBlockExit("vnc-rfbregion.c", 373);
  while (1) {
    if ((unsigned long )s_curr != (unsigned long )(& src->back)) {
      if (! ((unsigned long )d_curr != (unsigned long )(& dest->back))) {
        break;
      }
    } else {
      break;
    }
    logComment("Loop-iteration-start_vnc-rfbregion.c_374");
    {
    if (d_curr->start >= s_curr->end) {
      logBlockEntry("vnc-rfbregion.c", 378);
      s_curr = s_curr->_next;
      logAssignNoOffset("vnc-rfbregion.c", 378, & s_curr, "struct sraSpan *", "s_curr",
                        s_curr, "struct sraSpan", "s_curr->_next", 0);
      logBlockExit("vnc-rfbregion.c", 378);
      {
      logComment("Loop-iteration-end");
      continue;
      }
    }
    if (d_curr->end <= s_curr->start) {
      logBlockEntry("vnc-rfbregion.c", 384);
      next = d_curr->_next;
      logAssignNoOffset("vnc-rfbregion.c", 384, & next, "struct sraSpan *", "next",
                        next, "struct sraSpan", "d_curr->_next", 0);
      logBlockExit("vnc-rfbregion.c", 385);
      sraSpanRemove(d_curr);
      logBlockEntry("", -1);
      dumpVarsOutOfScope();
      logBlockExit("vnc-rfbregion.c", 386);
      sraSpanDestroy(d_curr);
      logBlockEntry("", -1);
      dumpVarsOutOfScope();
      d_curr = next;
      logAssignNoOffset("vnc-rfbregion.c", 387, & d_curr, "struct sraSpan *", "d_curr",
                        d_curr, "struct sraSpan", "next", 0);
      logBlockExit("vnc-rfbregion.c", 387);
      {
      logComment("Loop-iteration-end");
      continue;
      }
    }
    if (s_curr->start > d_curr->start) {
      d_curr->start = s_curr->start;
    }
    if (s_curr->end < d_curr->end) {
      tmp = sraSpanCreate(s_curr->end, d_curr->end, (sraSpanList const   *)d_curr->subspan);
      logBlockEntry("vnc-rfbregion.c", 398);
      logAssignNoOffset("vnc-rfbregion.c", 398, & tmp, "struct sraSpan *", "tmp",
                        tmp, "struct sraSpan", "sraSpanCreate", 0);
      dumpVarsOutOfScope();
      logBlockExit("vnc-rfbregion.c", 398);
      sraSpanInsertAfter(tmp, d_curr);
      logBlockEntry("", -1);
      dumpVarsOutOfScope();
      d_curr->end = s_curr->end;
      logBlockExit("vnc-rfbregion.c", 402);
    }
    tmp___0 = sraSpanListAnd(d_curr->subspan, (sraSpanList const   *)s_curr->subspan);
    logBlockEntry("", -1);
    dumpVarsOutOfScope();
    logBlockExit("", -1);
    if (tmp___0) {
      if ((unsigned long )d_curr->_prev != (unsigned long )(& dest->front)) {
        sraSpanMergePrevious(d_curr);
        logBlockEntry("", -1);
        dumpVarsOutOfScope();
        logBlockExit("", -1);
      }
      logBlockEntry("vnc-rfbregion.c", 418);
      d_next = d_curr;
      logAssignNoOffset("vnc-rfbregion.c", 418, & d_next, "struct sraSpan *", "d_next",
                        d_next, "struct sraSpan", "d_curr", 0);
      logBlockExit("vnc-rfbregion.c", 418);
      if (s_curr->end >= d_curr->end) {
        logBlockEntry("vnc-rfbregion.c", 420);
        d_next = d_curr->_next;
        logAssignNoOffset("vnc-rfbregion.c", 420, & d_next, "struct sraSpan *", "d_next",
                          d_next, "struct sraSpan", "d_curr->_next", 0);
        logBlockExit("vnc-rfbregion.c", 420);
      }
      if (s_curr->end <= d_curr->end) {
        logBlockEntry("vnc-rfbregion.c", 423);
        s_curr = s_curr->_next;
        logAssignNoOffset("vnc-rfbregion.c", 423, & s_curr, "struct sraSpan *", "s_curr",
                          s_curr, "struct sraSpan", "s_curr->_next", 0);
        logBlockExit("vnc-rfbregion.c", 423);
      }
      logBlockEntry("vnc-rfbregion.c", 425);
      d_curr = d_next;
      logAssignNoOffset("vnc-rfbregion.c", 425, & d_curr, "struct sraSpan *", "d_curr",
                        d_curr, "struct sraSpan", "d_next", 0);
      logBlockExit("vnc-rfbregion.c", 425);
    } else {
      logBlockEntry("vnc-rfbregion.c", 408);
      next___0 = d_curr->_next;
      logAssignNoOffset("vnc-rfbregion.c", 408, & next___0, "struct sraSpan *", "next___0",
                        next___0, "struct sraSpan", "d_curr->_next", 0);
      logBlockExit("vnc-rfbregion.c", 409);
      sraSpanRemove(d_curr);
      logBlockEntry("", -1);
      dumpVarsOutOfScope();
      logBlockExit("vnc-rfbregion.c", 410);
      sraSpanDestroy(d_curr);
      logBlockEntry("", -1);
      dumpVarsOutOfScope();
      d_curr = next___0;
      logAssignNoOffset("vnc-rfbregion.c", 411, & d_curr, "struct sraSpan *", "d_curr",
                        d_curr, "struct sraSpan", "next___0", 0);
      logBlockExit("vnc-rfbregion.c", 411);
    }
    }
    logComment("Loop-iteration-end");
  }
  while ((unsigned long )d_curr != (unsigned long )(& dest->back)) {
    logComment("Loop-iteration-start_vnc-rfbregion.c_429");
    {
    logBlockEntry("vnc-rfbregion.c", 430);
    next___1 = d_curr->_next;
    logAssignNoOffset("vnc-rfbregion.c", 430, & next___1, "struct sraSpan *", "next___1",
                      next___1, "struct sraSpan", "d_curr->_next", 0);
    logBlockExit("vnc-rfbregion.c", 431);
    sraSpanRemove(d_curr);
    logBlockEntry("", -1);
    dumpVarsOutOfScope();
    logBlockExit("vnc-rfbregion.c", 432);
    sraSpanDestroy(d_curr);
    logBlockEntry("", -1);
    dumpVarsOutOfScope();
    d_curr = next___1;
    logAssignNoOffset("vnc-rfbregion.c", 433, & d_curr, "struct sraSpan *", "d_curr",
                      d_curr, "struct sraSpan", "next___1", 0);
    logBlockExit("vnc-rfbregion.c", 433);
    }
    logComment("Loop-iteration-end");
  }
  tmp___1 = sraSpanListEmpty((sraSpanList const   *)dest);
  logBlockEntry("", -1);
  dumpVarsOutOfScope();
  logBlockExit("", -1);
  if (tmp___1) {
    tmp___2 = 0;
  } else {
    tmp___2 = 1;
  }
  {
  logVarOutOfScope("dest", & dest);
  logVarOutOfScope("src", & src);
  logVarOutOfScope("d_curr", & d_curr);
  logVarOutOfScope("s_curr", & s_curr);
  logVarOutOfScope("d_next", & d_next);
  logVarOutOfScope("next", & next);
  logVarOutOfScope("tmp", & tmp);
  logVarOutOfScope("next___0", & next___0);
  logVarOutOfScope("next___1", & next___1);
  {
  logComment("Function Exited");
  return ((rfbBool )tmp___2);
  }
  }
}
}
static rfbBool sraSpanListSubtract(sraSpanList *dest , sraSpanList const   *src ) 
{ 
  sraSpan *d_curr ;
  sraSpan *s_curr ;
  sraSpan *tmp ;
  sraSpan *tmp___0 ;
  sraSpan *next ;
  rfbBool tmp___1 ;
  rfbBool tmp___2 ;
  int tmp___3 ;

  {
  logComment("Function Entered");
  logVarInScope("formal", "dest", "sraSpanList *", & dest);
  logVarInScope("formal", "src", "sraSpanList const   *", & src);
  logVarInScope("local", "d_curr", "sraSpan *", & d_curr);
  logVarInScope("local", "s_curr", "sraSpan *", & s_curr);
  logVarInScope("local", "tmp", "sraSpan *", & tmp);
  logVarInScope("local", "tmp___0", "sraSpan *", & tmp___0);
  logVarInScope("local", "next", "sraSpan *", & next);
  if (! dest) {
    if (! src) {
      {
      logVarOutOfScope("dest", & dest);
      logVarOutOfScope("src", & src);
      logVarOutOfScope("d_curr", & d_curr);
      logVarOutOfScope("s_curr", & s_curr);
      logVarOutOfScope("tmp", & tmp);
      logVarOutOfScope("tmp___0", & tmp___0);
      logVarOutOfScope("next", & next);
      {
      logComment("Function Exited");
      return ((rfbBool )1);
      }
      }
    } else {
      printf((char const   * __restrict  )"sraSpanListSubtract:incompatible spans (only one NULL!)\n");
      {
      logVarOutOfScope("dest", & dest);
      logVarOutOfScope("src", & src);
      logVarOutOfScope("d_curr", & d_curr);
      logVarOutOfScope("s_curr", & s_curr);
      logVarOutOfScope("tmp", & tmp);
      logVarOutOfScope("tmp___0", & tmp___0);
      logVarOutOfScope("next", & next);
      {
      logComment("Function Exited");
      return ((rfbBool )0);
      }
      }
    }
  }
  logBlockEntry("vnc-rfbregion.c", 452);
  d_curr = dest->front._next;
  logAssignNoOffset("vnc-rfbregion.c", 452, & d_curr, "struct sraSpan *", "d_curr",
                    d_curr, "struct sraSpan", "dest->front._next", 0);
  s_curr = (sraSpan *)src->front._next;
  logAssignNoOffset("vnc-rfbregion.c", 453, & s_curr, "struct sraSpan *", "s_curr",
                    s_curr, "struct sraSpan", "(sraSpan *)src->front._next", 0);
  logBlockExit("vnc-rfbregion.c", 453);
  while (1) {
    if ((unsigned long )s_curr != (unsigned long )(& src->back)) {
      if (! ((unsigned long )d_curr != (unsigned long )(& dest->back))) {
        break;
      }
    } else {
      break;
    }
    logComment("Loop-iteration-start_vnc-rfbregion.c_454");
    {
    if (d_curr->start >= s_curr->end) {
      logBlockEntry("vnc-rfbregion.c", 458);
      s_curr = s_curr->_next;
      logAssignNoOffset("vnc-rfbregion.c", 458, & s_curr, "struct sraSpan *", "s_curr",
                        s_curr, "struct sraSpan", "s_curr->_next", 0);
      logBlockExit("vnc-rfbregion.c", 458);
      {
      logComment("Loop-iteration-end");
      continue;
      }
    }
    if (d_curr->end <= s_curr->start) {
      logBlockEntry("vnc-rfbregion.c", 464);
      d_curr = d_curr->_next;
      logAssignNoOffset("vnc-rfbregion.c", 464, & d_curr, "struct sraSpan *", "d_curr",
                        d_curr, "struct sraSpan", "d_curr->_next", 0);
      logBlockExit("vnc-rfbregion.c", 464);
      {
      logComment("Loop-iteration-end");
      continue;
      }
    }
    if (s_curr->start > d_curr->start) {
      tmp = sraSpanCreate(d_curr->start, s_curr->start, (sraSpanList const   *)d_curr->subspan);
      logBlockEntry("vnc-rfbregion.c", 470);
      logAssignNoOffset("vnc-rfbregion.c", 470, & tmp, "struct sraSpan *", "tmp",
                        tmp, "struct sraSpan", "sraSpanCreate", 0);
      dumpVarsOutOfScope();
      logBlockExit("vnc-rfbregion.c", 470);
      sraSpanInsertBefore(tmp, d_curr);
      logBlockEntry("", -1);
      dumpVarsOutOfScope();
      d_curr->start = s_curr->start;
      logBlockExit("vnc-rfbregion.c", 474);
    }
    if (s_curr->end < d_curr->end) {
      tmp___0 = sraSpanCreate(s_curr->end, d_curr->end, (sraSpanList const   *)d_curr->subspan);
      logBlockEntry("vnc-rfbregion.c", 477);
      logAssignNoOffset("vnc-rfbregion.c", 477, & tmp___0, "struct sraSpan *", "tmp___0",
                        tmp___0, "struct sraSpan", "sraSpanCreate", 0);
      dumpVarsOutOfScope();
      logBlockExit("vnc-rfbregion.c", 477);
      sraSpanInsertAfter(tmp___0, d_curr);
      logBlockEntry("", -1);
      dumpVarsOutOfScope();
      d_curr->end = s_curr->end;
      logBlockExit("vnc-rfbregion.c", 481);
    }
    if (! d_curr->subspan) {
      logBlockEntry("vnc-rfbregion.c", 487);
      next = d_curr->_next;
      logAssignNoOffset("vnc-rfbregion.c", 487, & next, "struct sraSpan *", "next",
                        next, "struct sraSpan", "d_curr->_next", 0);
      logBlockExit("vnc-rfbregion.c", 488);
      sraSpanRemove(d_curr);
      logBlockEntry("", -1);
      dumpVarsOutOfScope();
      logBlockExit("vnc-rfbregion.c", 489);
      sraSpanDestroy(d_curr);
      logBlockEntry("", -1);
      dumpVarsOutOfScope();
      d_curr = next;
      logAssignNoOffset("vnc-rfbregion.c", 490, & d_curr, "struct sraSpan *", "d_curr",
                        d_curr, "struct sraSpan", "next", 0);
      logBlockExit("vnc-rfbregion.c", 490);
    } else {
      tmp___1 = sraSpanListSubtract(d_curr->subspan, (sraSpanList const   *)s_curr->subspan);
      logBlockEntry("", -1);
      dumpVarsOutOfScope();
      logBlockExit("", -1);
      if (tmp___1) {
        if ((unsigned long )d_curr->_prev != (unsigned long )(& dest->front)) {
          sraSpanMergePrevious(d_curr);
          logBlockEntry("", -1);
          dumpVarsOutOfScope();
          logBlockExit("", -1);
        }
        if ((unsigned long )d_curr->_next != (unsigned long )(& dest->back)) {
          sraSpanMergeNext(d_curr);
          logBlockEntry("", -1);
          dumpVarsOutOfScope();
          logBlockExit("", -1);
        }
        if (s_curr->end > d_curr->end) {
          logBlockEntry("vnc-rfbregion.c", 500);
          d_curr = d_curr->_next;
          logAssignNoOffset("vnc-rfbregion.c", 500, & d_curr, "struct sraSpan *",
                            "d_curr", d_curr, "struct sraSpan", "d_curr->_next", 0);
          logBlockExit("vnc-rfbregion.c", 500);
        } else {
          logBlockEntry("vnc-rfbregion.c", 502);
          s_curr = s_curr->_next;
          logAssignNoOffset("vnc-rfbregion.c", 502, & s_curr, "struct sraSpan *",
                            "s_curr", s_curr, "struct sraSpan", "s_curr->_next", 0);
          logBlockExit("vnc-rfbregion.c", 502);
        }
      } else {
        logBlockEntry("vnc-rfbregion.c", 487);
        next = d_curr->_next;
        logAssignNoOffset("vnc-rfbregion.c", 487, & next, "struct sraSpan *", "next",
                          next, "struct sraSpan", "d_curr->_next", 0);
        logBlockExit("vnc-rfbregion.c", 488);
        sraSpanRemove(d_curr);
        logBlockEntry("", -1);
        dumpVarsOutOfScope();
        logBlockExit("vnc-rfbregion.c", 489);
        sraSpanDestroy(d_curr);
        logBlockEntry("", -1);
        dumpVarsOutOfScope();
        d_curr = next;
        logAssignNoOffset("vnc-rfbregion.c", 490, & d_curr, "struct sraSpan *", "d_curr",
                          d_curr, "struct sraSpan", "next", 0);
        logBlockExit("vnc-rfbregion.c", 490);
      }
    }
    }
    logComment("Loop-iteration-end");
  }
  tmp___2 = sraSpanListEmpty((sraSpanList const   *)dest);
  logBlockEntry("", -1);
  dumpVarsOutOfScope();
  logBlockExit("", -1);
  if (tmp___2) {
    tmp___3 = 0;
  } else {
    tmp___3 = 1;
  }
  {
  logVarOutOfScope("dest", & dest);
  logVarOutOfScope("src", & src);
  logVarOutOfScope("d_curr", & d_curr);
  logVarOutOfScope("s_curr", & s_curr);
  logVarOutOfScope("tmp", & tmp);
  logVarOutOfScope("tmp___0", & tmp___0);
  logVarOutOfScope("next", & next);
  {
  logComment("Function Exited");
  return ((rfbBool )tmp___3);
  }
  }
}
}
sraRegion *sraRgnCreate(void) 
{ 
  sraSpanList *tmp ;

  {
  logComment("Function Entered");
  logVarInScope("local", "tmp", "sraSpanList *", & tmp);
  tmp = sraSpanListCreate();
  logBlockEntry("vnc-rfbregion.c", 514);
  logAssignNoOffset("vnc-rfbregion.c", 514, & tmp, "struct sraRegion *", "tmp", tmp,
                    "struct sraRegion", "sraSpanListCreate", 0);
  dumpVarsOutOfScope();
  logBlockExit("", -1);
  {
  logVarOutOfScope("tmp", & tmp);
  {
  logComment("Function Exited");
  return (tmp);
  }
  }
}
}
sraRegion *sraRgnCreateRect(int x1 , int y1 , int x2 , int y2 ) 
{ 
  sraSpanList *vlist ;
  sraSpanList *hlist ;
  sraSpan *vspan ;
  sraSpan *hspan ;

  {
  logComment("Function Entered");
  logVarInScope("local", "vlist", "sraSpanList *", & vlist);
  logVarInScope("local", "hlist", "sraSpanList *", & hlist);
  logVarInScope("local", "vspan", "sraSpan *", & vspan);
  logVarInScope("local", "hspan", "sraSpan *", & hspan);
  hlist = sraSpanListCreate();
  logBlockEntry("vnc-rfbregion.c", 523);
  logAssignNoOffset("vnc-rfbregion.c", 523, & hlist, "struct sraRegion *", "hlist",
                    hlist, "struct sraRegion", "sraSpanListCreate", 0);
  dumpVarsOutOfScope();
  logBlockExit("vnc-rfbregion.c", 524);
  hspan = sraSpanCreate(x1, x2, (sraSpanList const   *)((void *)0));
  logBlockEntry("vnc-rfbregion.c", 524);
  logAssignNoOffset("vnc-rfbregion.c", 524, & hspan, "struct sraSpan *", "hspan",
                    hspan, "struct sraSpan", "sraSpanCreate", 0);
  dumpVarsOutOfScope();
  logBlockExit("vnc-rfbregion.c", 525);
  sraSpanInsertAfter(hspan, & hlist->front);
  logBlockEntry("", -1);
  dumpVarsOutOfScope();
  logBlockExit("vnc-rfbregion.c", 528);
  vlist = sraSpanListCreate();
  logBlockEntry("vnc-rfbregion.c", 528);
  logAssignNoOffset("vnc-rfbregion.c", 528, & vlist, "struct sraRegion *", "vlist",
                    vlist, "struct sraRegion", "sraSpanListCreate", 0);
  dumpVarsOutOfScope();
  logBlockExit("vnc-rfbregion.c", 529);
  vspan = sraSpanCreate(y1, y2, (sraSpanList const   *)hlist);
  logBlockEntry("vnc-rfbregion.c", 529);
  logAssignNoOffset("vnc-rfbregion.c", 529, & vspan, "struct sraSpan *", "vspan",
                    vspan, "struct sraSpan", "sraSpanCreate", 0);
  dumpVarsOutOfScope();
  logBlockExit("vnc-rfbregion.c", 530);
  sraSpanInsertAfter(vspan, & vlist->front);
  logBlockEntry("", -1);
  dumpVarsOutOfScope();
  logBlockExit("vnc-rfbregion.c", 532);
  sraSpanListDestroy(hlist);
  logBlockEntry("", -1);
  dumpVarsOutOfScope();
  logBlockExit("", -1);
  {
  logVarOutOfScope("vlist", & vlist);
  logVarOutOfScope("hlist", & hlist);
  logVarOutOfScope("vspan", & vspan);
  logVarOutOfScope("hspan", & hspan);
  {
  logComment("Function Exited");
  return (vlist);
  }
  }
}
}
sraRegion *sraRgnCreateRgn(sraRegion const   *src ) 
{ 
  sraSpanList *tmp ;

  {
  logComment("Function Entered");
  logVarInScope("formal", "src", "sraRegion const   *", & src);
  logVarInScope("local", "tmp", "sraSpanList *", & tmp);
  tmp = sraSpanListDup((sraSpanList const   *)((sraSpanList *)src));
  logBlockEntry("vnc-rfbregion.c", 539);
  logAssignNoOffset("vnc-rfbregion.c", 539, & tmp, "struct sraRegion *", "tmp", tmp,
                    "struct sraRegion", "sraSpanListDup", 0);
  dumpVarsOutOfScope();
  logBlockExit("", -1);
  {
  logVarOutOfScope("src", & src);
  logVarOutOfScope("tmp", & tmp);
  {
  logComment("Function Exited");
  return (tmp);
  }
  }
}
}
void sraRgnDestroy(sraRegion *rgn ) 
{ 


  {
  logComment("Function Entered");
  logVarInScope("formal", "rgn", "sraRegion *", & rgn);
  sraSpanListDestroy(rgn);
  logBlockEntry("", -1);
  dumpVarsOutOfScope();
  logBlockExit("", -1);
  {
  logVarOutOfScope("rgn", & rgn);
  {
  logComment("Function Exited");
  return;
  }
  }
}
}
void sraRgnMakeEmpty(sraRegion *rgn ) 
{ 


  {
  logComment("Function Entered");
  logVarInScope("formal", "rgn", "sraRegion *", & rgn);
  sraSpanListMakeEmpty(rgn);
  logBlockEntry("", -1);
  dumpVarsOutOfScope();
  logBlockExit("", -1);
  {
  logVarOutOfScope("rgn", & rgn);
  {
  logComment("Function Exited");
  return;
  }
  }
}
}
rfbBool sraRgnAnd(sraRegion *dst , sraRegion const   *src ) 
{ 
  rfbBool tmp ;

  {
  logComment("Function Entered");
  logVarInScope("formal", "dst", "sraRegion *", & dst);
  logVarInScope("formal", "src", "sraRegion const   *", & src);
  tmp = sraSpanListAnd(dst, (sraSpanList const   *)((sraSpanList *)src));
  logBlockEntry("", -1);
  dumpVarsOutOfScope();
  logBlockExit("", -1);
  {
  logVarOutOfScope("dst", & dst);
  logVarOutOfScope("src", & src);
  {
  logComment("Function Exited");
  return (tmp);
  }
  }
}
}
void sraRgnOr(sraRegion *dst , sraRegion const   *src ) 
{ 


  {
  logComment("Function Entered");
  logVarInScope("formal", "dst", "sraRegion *", & dst);
  logVarInScope("formal", "src", "sraRegion const   *", & src);
  sraSpanListOr(dst, (sraSpanList const   *)((sraSpanList *)src));
  logBlockEntry("", -1);
  dumpVarsOutOfScope();
  logBlockExit("", -1);
  {
  logVarOutOfScope("dst", & dst);
  logVarOutOfScope("src", & src);
  {
  logComment("Function Exited");
  return;
  }
  }
}
}
rfbBool sraRgnSubtract(sraRegion *dst , sraRegion const   *src ) 
{ 
  rfbBool tmp ;

  {
  logComment("Function Entered");
  logVarInScope("formal", "dst", "sraRegion *", & dst);
  logVarInScope("formal", "src", "sraRegion const   *", & src);
  tmp = sraSpanListSubtract(dst, (sraSpanList const   *)((sraSpanList *)src));
  logBlockEntry("", -1);
  dumpVarsOutOfScope();
  logBlockExit("", -1);
  {
  logVarOutOfScope("dst", & dst);
  logVarOutOfScope("src", & src);
  {
  logComment("Function Exited");
  return (tmp);
  }
  }
}
}
void sraRgnOffset(sraRegion *dst , int dx , int dy ) 
{ 
  sraSpan *vcurr ;
  sraSpan *hcurr ;

  {
  logComment("Function Entered");
  logVarInScope("formal", "dst", "sraRegion *", & dst);
  logVarInScope("local", "vcurr", "sraSpan *", & vcurr);
  logVarInScope("local", "hcurr", "sraSpan *", & hcurr);
  logBlockEntry("vnc-rfbregion.c", 573);
  vcurr = dst->front._next;
  logAssignNoOffset("vnc-rfbregion.c", 573, & vcurr, "struct sraSpan *", "vcurr",
                    vcurr, "struct sraSpan", "dst->front._next", 0);
  logBlockExit("vnc-rfbregion.c", 573);
  while ((unsigned long )vcurr != (unsigned long )(& dst->back)) {
    logComment("Loop-iteration-start_vnc-rfbregion.c_574");
    {
    logBlockEntry("vnc-rfbregion.c", 575);
    vcurr->start += dy;
    vcurr->end += dy;
    hcurr = (vcurr->subspan)->front._next;
    logAssignNoOffset("vnc-rfbregion.c", 578, & hcurr, "struct sraSpan *", "hcurr",
                      hcurr, "struct sraSpan", "(vcurr->subspan)->front._next", 0);
    logBlockExit("vnc-rfbregion.c", 578);
    while (1) {
      if (! ((unsigned long )hcurr != (unsigned long )(& (vcurr->subspan)->back))) {
        {
        logComment("Loop-iteration-end");
        break;
        }
      }
      logBlockEntry("vnc-rfbregion.c", 580);
      hcurr->start += dx;
      hcurr->end += dx;
      hcurr = hcurr->_next;
      logAssignNoOffset("vnc-rfbregion.c", 582, & hcurr, "struct sraSpan *", "hcurr",
                        hcurr, "struct sraSpan", "hcurr->_next", 0);
      logBlockExit("vnc-rfbregion.c", 582);
    }
    logBlockEntry("vnc-rfbregion.c", 585);
    vcurr = vcurr->_next;
    logAssignNoOffset("vnc-rfbregion.c", 585, & vcurr, "struct sraSpan *", "vcurr",
                      vcurr, "struct sraSpan", "vcurr->_next", 0);
    logBlockExit("vnc-rfbregion.c", 585);
    }
    logComment("Loop-iteration-end");
  }
  {
  logVarOutOfScope("dst", & dst);
  logVarOutOfScope("vcurr", & vcurr);
  logVarOutOfScope("hcurr", & hcurr);
  {
  logComment("Function Exited");
  return;
  }
  }
}
}
sraRegion *sraRgnBBox(sraRegion const   *src ) 
{ 
  int xmin ;
  int ymin ;
  int xmax ;
  int ymax ;
  sraSpan *vcurr ;
  sraSpan *hcurr ;
  sraRegion *tmp ;
  sraRegion *tmp___0 ;
  sraRegion *tmp___1 ;

  {
  logComment("Function Entered");
  logVarInScope("formal", "src", "sraRegion const   *", & src);
  logVarInScope("local", "vcurr", "sraSpan *", & vcurr);
  logVarInScope("local", "hcurr", "sraSpan *", & hcurr);
  logVarInScope("local", "tmp", "sraRegion *", & tmp);
  logVarInScope("local", "tmp___0", "sraRegion *", & tmp___0);
  logVarInScope("local", "tmp___1", "sraRegion *", & tmp___1);
  xmin = (int )(4294967295U >> 1);
  ymin = xmin;
  xmax = 1 - xmin;
  ymax = xmax;
  if (! src) {
    tmp = sraRgnCreate();
    logBlockEntry("vnc-rfbregion.c", 594);
    logAssignNoOffset("vnc-rfbregion.c", 594, & tmp, "struct sraRegion *", "tmp",
                      tmp, "struct sraRegion", "sraRgnCreate", 0);
    dumpVarsOutOfScope();
    logBlockExit("", -1);
    {
    logVarOutOfScope("src", & src);
    logVarOutOfScope("vcurr", & vcurr);
    logVarOutOfScope("hcurr", & hcurr);
    logVarOutOfScope("tmp", & tmp);
    logVarOutOfScope("tmp___0", & tmp___0);
    logVarOutOfScope("tmp___1", & tmp___1);
    {
    logComment("Function Exited");
    return (tmp);
    }
    }
  }
  logBlockEntry("vnc-rfbregion.c", 596);
  vcurr = ((sraSpanList *)src)->front._next;
  logAssignNoOffset("vnc-rfbregion.c", 596, & vcurr, "struct sraSpan *", "vcurr",
                    vcurr, "struct sraSpan", "((sraSpanList *)src)->front._next",
                    0);
  logBlockExit("vnc-rfbregion.c", 596);
  while ((unsigned long )vcurr != (unsigned long )(& ((sraSpanList *)src)->back)) {
    logComment("Loop-iteration-start_vnc-rfbregion.c_597");
    {
    if (vcurr->start < ymin) {
      ymin = vcurr->start;
    }
    if (vcurr->end > ymax) {
      ymax = vcurr->end;
    }
    logBlockEntry("vnc-rfbregion.c", 603);
    hcurr = (vcurr->subspan)->front._next;
    logAssignNoOffset("vnc-rfbregion.c", 603, & hcurr, "struct sraSpan *", "hcurr",
                      hcurr, "struct sraSpan", "(vcurr->subspan)->front._next", 0);
    logBlockExit("vnc-rfbregion.c", 603);
    while (1) {
      if (! ((unsigned long )hcurr != (unsigned long )(& (vcurr->subspan)->back))) {
        {
        logComment("Loop-iteration-end");
        break;
        }
      }
      if (hcurr->start < xmin) {
        xmin = hcurr->start;
      }
      if (hcurr->end > xmax) {
        xmax = hcurr->end;
      }
      logBlockEntry("vnc-rfbregion.c", 609);
      hcurr = hcurr->_next;
      logAssignNoOffset("vnc-rfbregion.c", 609, & hcurr, "struct sraSpan *", "hcurr",
                        hcurr, "struct sraSpan", "hcurr->_next", 0);
      logBlockExit("vnc-rfbregion.c", 609);
    }
    logBlockEntry("vnc-rfbregion.c", 612);
    vcurr = vcurr->_next;
    logAssignNoOffset("vnc-rfbregion.c", 612, & vcurr, "struct sraSpan *", "vcurr",
                      vcurr, "struct sraSpan", "vcurr->_next", 0);
    logBlockExit("vnc-rfbregion.c", 612);
    }
    logComment("Loop-iteration-end");
  }
  if (xmax < xmin) {
    tmp___0 = sraRgnCreate();
    logBlockEntry("vnc-rfbregion.c", 616);
    logAssignNoOffset("vnc-rfbregion.c", 616, & tmp___0, "struct sraRegion *", "tmp___0",
                      tmp___0, "struct sraRegion", "sraRgnCreate", 0);
    dumpVarsOutOfScope();
    logBlockExit("", -1);
    {
    logVarOutOfScope("src", & src);
    logVarOutOfScope("vcurr", & vcurr);
    logVarOutOfScope("hcurr", & hcurr);
    logVarOutOfScope("tmp", & tmp);
    logVarOutOfScope("tmp___0", & tmp___0);
    logVarOutOfScope("tmp___1", & tmp___1);
    {
    logComment("Function Exited");
    return (tmp___0);
    }
    }
  } else
  if (ymax < ymin) {
    tmp___0 = sraRgnCreate();
    logBlockEntry("vnc-rfbregion.c", 616);
    logAssignNoOffset("vnc-rfbregion.c", 616, & tmp___0, "struct sraRegion *", "tmp___0",
                      tmp___0, "struct sraRegion", "sraRgnCreate", 0);
    dumpVarsOutOfScope();
    logBlockExit("", -1);
    {
    logVarOutOfScope("src", & src);
    logVarOutOfScope("vcurr", & vcurr);
    logVarOutOfScope("hcurr", & hcurr);
    logVarOutOfScope("tmp", & tmp);
    logVarOutOfScope("tmp___0", & tmp___0);
    logVarOutOfScope("tmp___1", & tmp___1);
    {
    logComment("Function Exited");
    return (tmp___0);
    }
    }
  }
  tmp___1 = sraRgnCreateRect(xmin, ymin, xmax, ymax);
  logBlockEntry("vnc-rfbregion.c", 618);
  logAssignNoOffset("vnc-rfbregion.c", 618, & tmp___1, "struct sraRegion *", "tmp___1",
                    tmp___1, "struct sraRegion", "sraRgnCreateRect", 0);
  dumpVarsOutOfScope();
  logBlockExit("", -1);
  {
  logVarOutOfScope("src", & src);
  logVarOutOfScope("vcurr", & vcurr);
  logVarOutOfScope("hcurr", & hcurr);
  logVarOutOfScope("tmp", & tmp);
  logVarOutOfScope("tmp___0", & tmp___0);
  logVarOutOfScope("tmp___1", & tmp___1);
  {
  logComment("Function Exited");
  return (tmp___1);
  }
  }
}
}
rfbBool sraRgnPopRect(sraRegion *rgn , sraRect *rect , unsigned long flags ) 
{ 
  sraSpan *vcurr ;
  sraSpan *hcurr ;
  sraSpan *vend ;
  sraSpan *hend ;
  rfbBool right2left ;
  rfbBool bottom2top ;
  rfbBool tmp ;

  {
  logComment("Function Entered");
  logVarInScope("formal", "rgn", "sraRegion *", & rgn);
  logVarInScope("formal", "rect", "sraRect *", & rect);
  logVarInScope("local", "vcurr", "sraSpan *", & vcurr);
  logVarInScope("local", "hcurr", "sraSpan *", & hcurr);
  logVarInScope("local", "vend", "sraSpan *", & vend);
  logVarInScope("local", "hend", "sraSpan *", & hend);
  right2left = (rfbBool )((flags & 2UL) == 2UL);
  bottom2top = (rfbBool )((flags & 1UL) == 1UL);
  if (bottom2top) {
    logBlockEntry("vnc-rfbregion.c", 630);
    vcurr = rgn->back._prev;
    logAssignNoOffset("vnc-rfbregion.c", 630, & vcurr, "struct sraSpan *", "vcurr",
                      vcurr, "struct sraSpan", "rgn->back._prev", 0);
    vend = & rgn->front;
    logAssignNoOffset("vnc-rfbregion.c", 631, & vend, "struct sraSpan *", "vend",
                      vend, "struct sraSpan", "& rgn->front", 0);
    logBlockExit("vnc-rfbregion.c", 631);
  } else {
    logBlockEntry("vnc-rfbregion.c", 633);
    vcurr = rgn->front._next;
    logAssignNoOffset("vnc-rfbregion.c", 633, & vcurr, "struct sraSpan *", "vcurr",
                      vcurr, "struct sraSpan", "rgn->front._next", 0);
    vend = & rgn->back;
    logAssignNoOffset("vnc-rfbregion.c", 634, & vend, "struct sraSpan *", "vend",
                      vend, "struct sraSpan", "& rgn->back", 0);
    logBlockExit("vnc-rfbregion.c", 634);
  }
  if ((unsigned long )vcurr != (unsigned long )vend) {
    rect->y1 = vcurr->start;
    rect->y2 = vcurr->end;
    if (right2left) {
      logBlockEntry("vnc-rfbregion.c", 643);
      hcurr = (vcurr->subspan)->back._prev;
      logAssignNoOffset("vnc-rfbregion.c", 643, & hcurr, "struct sraSpan *", "hcurr",
                        hcurr, "struct sraSpan", "(vcurr->subspan)->back._prev", 0);
      hend = & (vcurr->subspan)->front;
      logAssignNoOffset("vnc-rfbregion.c", 644, & hend, "struct sraSpan *", "hend",
                        hend, "struct sraSpan", "& (vcurr->subspan)->front", 0);
      logBlockExit("vnc-rfbregion.c", 644);
    } else {
      logBlockEntry("vnc-rfbregion.c", 646);
      hcurr = (vcurr->subspan)->front._next;
      logAssignNoOffset("vnc-rfbregion.c", 646, & hcurr, "struct sraSpan *", "hcurr",
                        hcurr, "struct sraSpan", "(vcurr->subspan)->front._next",
                        0);
      hend = & (vcurr->subspan)->back;
      logAssignNoOffset("vnc-rfbregion.c", 647, & hend, "struct sraSpan *", "hend",
                        hend, "struct sraSpan", "& (vcurr->subspan)->back", 0);
      logBlockExit("vnc-rfbregion.c", 647);
    }
    if ((unsigned long )hcurr != (unsigned long )hend) {
      logBlockEntry("vnc-rfbregion.c", 651);
      rect->x1 = hcurr->start;
      rect->x2 = hcurr->end;
      logBlockExit("vnc-rfbregion.c", 654);
      sraSpanRemove(hcurr);
      logBlockEntry("", -1);
      dumpVarsOutOfScope();
      logBlockExit("vnc-rfbregion.c", 655);
      sraSpanDestroy(hcurr);
      logBlockEntry("", -1);
      dumpVarsOutOfScope();
      logBlockExit("vnc-rfbregion.c", 657);
      tmp = sraSpanListEmpty((sraSpanList const   *)vcurr->subspan);
      logBlockEntry("", -1);
      dumpVarsOutOfScope();
      logBlockExit("", -1);
      if (tmp) {
        sraSpanRemove(vcurr);
        logBlockEntry("", -1);
        dumpVarsOutOfScope();
        logBlockExit("vnc-rfbregion.c", 659);
        sraSpanDestroy(vcurr);
        logBlockEntry("", -1);
        dumpVarsOutOfScope();
        logBlockExit("", -1);
      }
      {
      logVarOutOfScope("rgn", & rgn);
      logVarOutOfScope("rect", & rect);
      logVarOutOfScope("vcurr", & vcurr);
      logVarOutOfScope("hcurr", & hcurr);
      logVarOutOfScope("vend", & vend);
      logVarOutOfScope("hend", & hend);
      {
      logComment("Function Exited");
      return ((rfbBool )1);
      }
      }
    }
  }
  {
  logVarOutOfScope("rgn", & rgn);
  logVarOutOfScope("rect", & rect);
  logVarOutOfScope("vcurr", & vcurr);
  logVarOutOfScope("hcurr", & hcurr);
  logVarOutOfScope("vend", & vend);
  logVarOutOfScope("hend", & hend);
  {
  logComment("Function Exited");
  return ((rfbBool )0);
  }
  }
}
}
unsigned long sraRgnCountRects(sraRegion const   *rgn ) 
{ 
  unsigned long count ;
  unsigned long tmp ;

  {
  logComment("Function Entered");
  logVarInScope("formal", "rgn", "sraRegion const   *", & rgn);
  tmp = sraSpanListCount((sraSpanList const   *)((sraSpanList *)rgn));
  logBlockEntry("", -1);
  dumpVarsOutOfScope();
  count = tmp;
  logBlockExit("vnc-rfbregion.c", 675);
  {
  logVarOutOfScope("rgn", & rgn);
  {
  logComment("Function Exited");
  return (count);
  }
  }
}
}
rfbBool sraRgnEmpty(sraRegion const   *rgn ) 
{ 
  rfbBool tmp ;

  {
  logComment("Function Entered");
  logVarInScope("formal", "rgn", "sraRegion const   *", & rgn);
  tmp = sraSpanListEmpty((sraSpanList const   *)((sraSpanList *)rgn));
  logBlockEntry("", -1);
  dumpVarsOutOfScope();
  logBlockExit("", -1);
  {
  logVarOutOfScope("rgn", & rgn);
  {
  logComment("Function Exited");
  return (tmp);
  }
  }
}
}
sraRectangleIterator *sraRgnGetIterator(sraRegion *s ) 
{ 
  sraRectangleIterator *i ;
  void *tmp ;
  void *tmp___0 ;

  {
  logComment("Function Entered");
  logVarInScope("formal", "s", "sraRegion *", & s);
  logVarInScope("local", "i", "sraRectangleIterator *", & i);
  logVarInScope("local", "tmp", "void *", & tmp);
  logVarInScope("local", "tmp___0", "void *", & tmp___0);
  logBlockEntry("vnc-rfbregion.c", 690);
  tmp = malloc(sizeof(sraRectangleIterator ));
  logAssignNoOffset("vnc-rfbregion.c", 690, & tmp, "void *", "tmp", tmp, "void", "malloc",
                    1);
  logMallocInfo("sizeof(sraRectangleIterator )", sizeof(sraRectangleIterator ));
  i = (sraRectangleIterator *)tmp;
  logAssignNoOffset("vnc-rfbregion.c", 690, & i, "struct sraRectangleIterator *",
                    "i", i, "struct sraRectangleIterator", "(sraRectangleIterator *)tmp",
                    0);
  logBlockExit("vnc-rfbregion.c", 690);
  if (! i) {
    {
    logVarOutOfScope("s", & s);
    logVarOutOfScope("i", & i);
    logVarOutOfScope("tmp", & tmp);
    logVarOutOfScope("tmp___0", & tmp___0);
    {
    logComment("Function Exited");
    return ((sraRectangleIterator *)((void *)0));
    }
    }
  }
  logBlockEntry("vnc-rfbregion.c", 699);
  tmp___0 = malloc(sizeof(sraSpan *) * 4UL);
  logAssignNoOffset("vnc-rfbregion.c", 699, & tmp___0, "void *", "tmp___0", tmp___0,
                    "void", "malloc", 1);
  logMallocInfo("sizeof(sraSpan *) * 4UL", sizeof(sraSpan *) * 4UL);
  i->sPtrs = (sraSpan **)tmp___0;
  logAssignOffset("vnc-rfbregion.c", 699, i, "struct sraRectangleIterator", "*i",
                  & i->sPtrs, "struct sraSpan **", "i->sPtrs", i->sPtrs, "struct sraSpan *",
                  "(sraSpan **)tmp___0", 0);
  logBlockExit("vnc-rfbregion.c", 699);
  if (! i->sPtrs) {
    logBlockEntry("", -1);
    logFree("vnc-rfbregion.c", 701, "(void *)i", (void *)i);
    free((void *)i);
    logBlockExit("vnc-rfbregion.c", 701);
    {
    logVarOutOfScope("s", & s);
    logVarOutOfScope("i", & i);
    logVarOutOfScope("tmp", & tmp);
    logVarOutOfScope("tmp___0", & tmp___0);
    {
    logComment("Function Exited");
    return ((sraRectangleIterator *)((void *)0));
    }
    }
  }
  logBlockEntry("vnc-rfbregion.c", 704);
  i->ptrSize = 4;
  *(i->sPtrs + 0) = & s->front;
  logAssignNoOffset("vnc-rfbregion.c", 705, & *(i->sPtrs + 0), "struct sraSpan *",
                    "*(i->sPtrs + 0)", *(i->sPtrs + 0), "struct sraSpan", "& s->front",
                    0);
  *(i->sPtrs + 1) = & s->back;
  logAssignNoOffset("vnc-rfbregion.c", 706, & *(i->sPtrs + 1), "struct sraSpan *",
                    "*(i->sPtrs + 1)", *(i->sPtrs + 1), "struct sraSpan", "& s->back",
                    0);
  i->ptrPos = 0;
  i->reverseX = (rfbBool )0;
  i->reverseY = (rfbBool )0;
  logBlockExit("vnc-rfbregion.c", 709);
  {
  logVarOutOfScope("s", & s);
  logVarOutOfScope("i", & i);
  logVarOutOfScope("tmp", & tmp);
  logVarOutOfScope("tmp___0", & tmp___0);
  {
  logComment("Function Exited");
  return (i);
  }
  }
}
}
sraRectangleIterator *sraRgnGetReverseIterator(sraRegion *s , rfbBool reverseX , rfbBool reverseY ) 
{ 
  sraRectangleIterator *i ;
  sraRectangleIterator *tmp ;

  {
  logComment("Function Entered");
  logVarInScope("formal", "s", "sraRegion *", & s);
  logVarInScope("local", "i", "sraRectangleIterator *", & i);
  logVarInScope("local", "tmp", "sraRectangleIterator *", & tmp);
  tmp = sraRgnGetIterator(s);
  logBlockEntry("vnc-rfbregion.c", 715);
  logAssignNoOffset("vnc-rfbregion.c", 715, & tmp, "struct sraRectangleIterator *",
                    "tmp", tmp, "struct sraRectangleIterator", "sraRgnGetIterator",
                    0);
  dumpVarsOutOfScope();
  i = tmp;
  logAssignNoOffset("vnc-rfbregion.c", 715, & i, "struct sraRectangleIterator *",
                    "i", i, "struct sraRectangleIterator", "tmp", 0);
  logBlockExit("vnc-rfbregion.c", 715);
  if (reverseY) {
    logBlockEntry("vnc-rfbregion.c", 717);
    *(i->sPtrs + 1) = & s->front;
    logAssignNoOffset("vnc-rfbregion.c", 717, & *(i->sPtrs + 1), "struct sraSpan *",
                      "*(i->sPtrs + 1)", *(i->sPtrs + 1), "struct sraSpan", "& s->front",
                      0);
    *(i->sPtrs + 0) = & s->back;
    logAssignNoOffset("vnc-rfbregion.c", 718, & *(i->sPtrs + 0), "struct sraSpan *",
                      "*(i->sPtrs + 0)", *(i->sPtrs + 0), "struct sraSpan", "& s->back",
                      0);
    logBlockExit("vnc-rfbregion.c", 718);
  }
  i->reverseX = reverseX;
  i->reverseY = reverseY;
  {
  logVarOutOfScope("s", & s);
  logVarOutOfScope("i", & i);
  logVarOutOfScope("tmp", & tmp);
  {
  logComment("Function Exited");
  return (i);
  }
  }
}
}
static rfbBool sraReverse(sraRectangleIterator *i ) 
{ 
  int tmp ;

  {
  logComment("Function Entered");
  logVarInScope("formal", "i", "sraRectangleIterator *", & i);
  if (i->ptrPos & 2) {
    if (i->reverseX) {
      tmp = 1;
    } else {
      goto _L;
    }
  } else
  _L: /* CIL Label */ 
  if (! (i->ptrPos & 2)) {
    if (i->reverseY) {
      tmp = 1;
    } else {
      tmp = 0;
    }
  } else {
    tmp = 0;
  }
  {
  logVarOutOfScope("i", & i);
  {
  logComment("Function Exited");
  return ((rfbBool )tmp);
  }
  }
}
}
static sraSpan *sraNextSpan(sraRectangleIterator *i ) 
{ 
  rfbBool tmp ;

  {
  logComment("Function Entered");
  logVarInScope("formal", "i", "sraRectangleIterator *", & i);
  tmp = sraReverse(i);
  logBlockEntry("", -1);
  dumpVarsOutOfScope();
  logBlockExit("", -1);
  if (tmp) {
    {
    logVarOutOfScope("i", & i);
    {
    logComment("Function Exited");
    return ((*(i->sPtrs + i->ptrPos))->_prev);
    }
    }
  } else {
    {
    logVarOutOfScope("i", & i);
    {
    logComment("Function Exited");
    return ((*(i->sPtrs + i->ptrPos))->_next);
    }
    }
  }
}
}
rfbBool sraRgnIteratorNext(sraRectangleIterator *i , sraRect *r ) 
{ 
  sraSpan *tmp ;
  void *tmp___0 ;
  rfbBool tmp___1 ;

  {
  logComment("Function Entered");
  logVarInScope("formal", "i", "sraRectangleIterator *", & i);
  logVarInScope("formal", "r", "sraRect *", & r);
  logVarInScope("local", "tmp", "sraSpan *", & tmp);
  logVarInScope("local", "tmp___0", "void *", & tmp___0);
  while (1) {
    tmp = sraNextSpan(i);
    logBlockEntry("vnc-rfbregion.c", 742);
    logAssignNoOffset("vnc-rfbregion.c", 742, & tmp, "struct sraSpan *", "tmp", tmp,
                      "struct sraSpan", "sraNextSpan", 0);
    dumpVarsOutOfScope();
    logBlockExit("", -1);
    logComment("Loop-iteration-start_vnc-rfbregion.c_742");
    {
    if (! ((unsigned long )tmp == (unsigned long )*(i->sPtrs + (i->ptrPos + 1)))) {
      {
      logComment("Loop-iteration-end");
      break;
      }
    }
    i->ptrPos -= 2;
    if (i->ptrPos < 0) {
      {
      logVarOutOfScope("i", & i);
      logVarOutOfScope("r", & r);
      logVarOutOfScope("tmp", & tmp);
      logVarOutOfScope("tmp___0", & tmp___0);
      {
      logComment("Function Exited");
      return ((rfbBool )0);
      }
      }
    }
    }
    logComment("Loop-iteration-end");
  }
  *(i->sPtrs + i->ptrPos) = sraNextSpan(i);
  logBlockEntry("vnc-rfbregion.c", 748);
  logAssignNoOffset("vnc-rfbregion.c", 748, & *(i->sPtrs + i->ptrPos), "struct sraSpan *",
                    "*(i->sPtrs + i->ptrPos)", *(i->sPtrs + i->ptrPos), "struct sraSpan",
                    "sraNextSpan", 0);
  dumpVarsOutOfScope();
  logBlockExit("", -1);
  while ((*(i->sPtrs + i->ptrPos))->subspan) {
    logComment("Loop-iteration-start_vnc-rfbregion.c_751");
    {
    if (i->ptrPos + 2 > i->ptrSize) {
      logBlockEntry("vnc-rfbregion.c", 753);
      i->ptrSize += 8;
      logBlockExit("vnc-rfbregion.c", 754);
      tmp___0 = realloc((void *)i->sPtrs, sizeof(sraSpan *) * (unsigned long )i->ptrSize);
      logBlockEntry("vnc-rfbregion.c", 754);
      logAssignNoOffset("vnc-rfbregion.c", 754, & tmp___0, "void *", "tmp___0", tmp___0,
                        "void", "realloc", 0);
      dumpVarsOutOfScope();
      i->sPtrs = (sraSpan **)tmp___0;
      logAssignOffset("vnc-rfbregion.c", 754, i, "struct sraRectangleIterator", "*i",
                      & i->sPtrs, "struct sraSpan **", "i->sPtrs", i->sPtrs, "struct sraSpan *",
                      "(sraSpan **)tmp___0", 0);
      logBlockExit("vnc-rfbregion.c", 754);
    }
    logBlockEntry("vnc-rfbregion.c", 756);
    i->ptrPos = 2;
    logBlockExit("vnc-rfbregion.c", 757);
    tmp___1 = sraReverse(i);
    logBlockEntry("", -1);
    dumpVarsOutOfScope();
    logBlockExit("", -1);
    if (tmp___1) {
      logBlockEntry("vnc-rfbregion.c", 758);
      *(i->sPtrs + i->ptrPos) = ((*(i->sPtrs + (i->ptrPos - 2)))->subspan)->back._prev;
      logAssignNoOffset("vnc-rfbregion.c", 758, & *(i->sPtrs + i->ptrPos), "struct sraSpan *",
                        "*(i->sPtrs + i->ptrPos)", *(i->sPtrs + i->ptrPos), "struct sraSpan",
                        "((*(i->sPtrs + (i->ptrPos - 2)))->subspan)->back._prev",
                        0);
      *(i->sPtrs + (i->ptrPos + 1)) = & ((*(i->sPtrs + (i->ptrPos - 2)))->subspan)->front;
      logAssignNoOffset("vnc-rfbregion.c", 759, & *(i->sPtrs + (i->ptrPos + 1)), "struct sraSpan *",
                        "*(i->sPtrs + (i->ptrPos + 1))", *(i->sPtrs + (i->ptrPos + 1)),
                        "struct sraSpan", "& ((*(i->sPtrs + (i->ptrPos - 2)))->subspan)->front",
                        0);
      logBlockExit("vnc-rfbregion.c", 759);
    } else {
      logBlockEntry("vnc-rfbregion.c", 761);
      *(i->sPtrs + i->ptrPos) = ((*(i->sPtrs + (i->ptrPos - 2)))->subspan)->front._next;
      logAssignNoOffset("vnc-rfbregion.c", 761, & *(i->sPtrs + i->ptrPos), "struct sraSpan *",
                        "*(i->sPtrs + i->ptrPos)", *(i->sPtrs + i->ptrPos), "struct sraSpan",
                        "((*(i->sPtrs + (i->ptrPos - 2)))->subspan)->front._next",
                        0);
      *(i->sPtrs + (i->ptrPos + 1)) = & ((*(i->sPtrs + (i->ptrPos - 2)))->subspan)->back;
      logAssignNoOffset("vnc-rfbregion.c", 762, & *(i->sPtrs + (i->ptrPos + 1)), "struct sraSpan *",
                        "*(i->sPtrs + (i->ptrPos + 1))", *(i->sPtrs + (i->ptrPos + 1)),
                        "struct sraSpan", "& ((*(i->sPtrs + (i->ptrPos - 2)))->subspan)->back",
                        0);
      logBlockExit("vnc-rfbregion.c", 762);
    }
    }
    logComment("Loop-iteration-end");
  }
  if (i->ptrPos % 4 != 2) {
    printf((char const   * __restrict  )"sraRgnIteratorNext: offset is wrong (%d%%4!=2)\n",
           i->ptrPos);
    {
    logVarOutOfScope("i", & i);
    logVarOutOfScope("r", & r);
    logVarOutOfScope("tmp", & tmp);
    logVarOutOfScope("tmp___0", & tmp___0);
    {
    logComment("Function Exited");
    return ((rfbBool )0);
    }
    }
  }
  r->y1 = (*(i->sPtrs + (i->ptrPos - 2)))->start;
  r->y2 = (*(i->sPtrs + (i->ptrPos - 2)))->end;
  r->x1 = (*(i->sPtrs + i->ptrPos))->start;
  r->x2 = (*(i->sPtrs + i->ptrPos))->end;
  {
  logVarOutOfScope("i", & i);
  logVarOutOfScope("r", & r);
  logVarOutOfScope("tmp", & tmp);
  logVarOutOfScope("tmp___0", & tmp___0);
  {
  logComment("Function Exited");
  return ((rfbBool )-1);
  }
  }
}
}
void sraRgnReleaseIterator(sraRectangleIterator *i ) 
{ 


  {
  logComment("Function Entered");
  logVarInScope("formal", "i", "sraRectangleIterator *", & i);
  logBlockEntry("", -1);
  logFree("vnc-rfbregion.c", 781, "(void *)i->sPtrs", (void *)i->sPtrs);
  free((void *)i->sPtrs);
  logFree("vnc-rfbregion.c", 782, "(void *)i", (void *)i);
  free((void *)i);
  logBlockExit("vnc-rfbregion.c", 782);
  {
  logVarOutOfScope("i", & i);
  {
  logComment("Function Exited");
  return;
  }
  }
}
}
void sraRgnPrint(sraRegion const   *rgn ) 
{ 


  {
  logComment("Function Entered");
  logVarInScope("formal", "rgn", "sraRegion const   *", & rgn);
  sraSpanListPrint((sraSpanList const   *)((sraSpanList *)rgn));
  logBlockEntry("", -1);
  dumpVarsOutOfScope();
  logBlockExit("", -1);
  {
  logVarOutOfScope("rgn", & rgn);
  {
  logComment("Function Exited");
  return;
  }
  }
}
}
rfbBool sraClipRect(int *x , int *y , int *w , int *h , int cx , int cy , int cw ,
                    int ch ) 
{ 
  int tmp ;

  {
  logComment("Function Entered");
  logVarInScope("formal", "x", "int *", & x);
  logVarInScope("formal", "y", "int *", & y);
  logVarInScope("formal", "w", "int *", & w);
  logVarInScope("formal", "h", "int *", & h);
  if (*x < cx) {
    *w -= cx - *x;
    *x = cx;
  }
  if (*y < cy) {
    *h -= cy - *y;
    *y = cy;
  }
  if (*x + *w > cx + cw) {
    *w = (cx + cw) - *x;
  }
  if (*y + *h > cy + ch) {
    *h = (cy + ch) - *y;
  }
  if (*w > 0) {
    if (*h > 0) {
      tmp = 1;
    } else {
      tmp = 0;
    }
  } else {
    tmp = 0;
  }
  {
  logVarOutOfScope("x", & x);
  logVarOutOfScope("y", & y);
  logVarOutOfScope("w", & w);
  logVarOutOfScope("h", & h);
  {
  logComment("Function Exited");
  return ((rfbBool )tmp);
  }
  }
}
}
rfbBool sraClipRect2(int *x , int *y , int *x2 , int *y2 , int cx , int cy , int cx2 ,
                     int cy2 ) 
{ 
  int tmp ;

  {
  logComment("Function Entered");
  logVarInScope("formal", "x", "int *", & x);
  logVarInScope("formal", "y", "int *", & y);
  logVarInScope("formal", "x2", "int *", & x2);
  logVarInScope("formal", "y2", "int *", & y2);
  if (*x < cx) {
    *x = cx;
  }
  if (*y < cy) {
    *y = cy;
  }
  if (*x >= cx2) {
    *x = cx2 - 1;
  }
  if (*y >= cy2) {
    *y = cy2 - 1;
  }
  if (*x2 <= cx) {
    *x2 = cx + 1;
  }
  if (*y2 <= cy) {
    *y2 = cy + 1;
  }
  if (*x2 > cx2) {
    *x2 = cx2;
  }
  if (*y2 > cy2) {
    *y2 = cy2;
  }
  if (*x2 > *x) {
    if (*y2 > *y) {
      tmp = 1;
    } else {
      tmp = 0;
    }
  } else {
    tmp = 0;
  }
  {
  logVarOutOfScope("x", & x);
  logVarOutOfScope("y", & y);
  logVarOutOfScope("x2", & x2);
  logVarOutOfScope("y2", & y2);
  {
  logComment("Function Exited");
  return ((rfbBool )tmp);
  }
  }
}
}
int main(int argc , char **argv ) 
{ 
  sraRegionPtr region ;
  sraRegionPtr region1 ;
  rfbBool b ;
  char const   *tmp ;

  {
  logComment("Function Entered");
  logVarInScope("formal", "argv", "char **", & argv);
  logVarInScope("local", "region", "sraRegionPtr", & region);
  logVarInScope("local", "region1", "sraRegionPtr", & region1);
  logVarInScope("local", "tmp", "char const   *", & tmp);
  region = sraRgnCreateRect(10, 10, 600, 300);
  logBlockEntry("vnc-rfbregion.c", 844);
  logAssignNoOffset("vnc-rfbregion.c", 844, & region, "struct sraRegion *", "region",
                    region, "struct sraRegion", "sraRgnCreateRect", 0);
  dumpVarsOutOfScope();
  logBlockExit("vnc-rfbregion.c", 845);
  region1 = sraRgnCreateRect(40, 50, 350, 200);
  logBlockEntry("vnc-rfbregion.c", 845);
  logAssignNoOffset("vnc-rfbregion.c", 845, & region1, "struct sraRegion *", "region1",
                    region1, "struct sraRegion", "sraRgnCreateRect", 0);
  dumpVarsOutOfScope();
  logBlockExit("vnc-rfbregion.c", 851);
  b = sraRgnSubtract(region, (sraRegion const   *)region1);
  logBlockEntry("", -1);
  dumpVarsOutOfScope();
  logBlockExit("", -1);
  if (b) {
    logBlockEntry("vnc-rfbregion.c", 852);
    tmp = "true";
    logAssignNoOffset("vnc-rfbregion.c", 852, & tmp, "char const   *", "tmp", tmp,
                      "char const", "\"true\"", 0);
    logBlockExit("vnc-rfbregion.c", 852);
  } else {
    logBlockEntry("vnc-rfbregion.c", 852);
    tmp = "false";
    logAssignNoOffset("vnc-rfbregion.c", 852, & tmp, "char const   *", "tmp", tmp,
                      "char const", "\"false\"", 0);
    logBlockExit("vnc-rfbregion.c", 852);
  }
  printf((char const   * __restrict  )"%s ", tmp);
  sraRgnDestroy(region);
  logBlockEntry("", -1);
  dumpVarsOutOfScope();
  logBlockExit("vnc-rfbregion.c", 886);
  sraRgnDestroy(region1);
  logBlockEntry("", -1);
  dumpVarsOutOfScope();
  logBlockExit("", -1);
  {
  logVarOutOfScope("argv", & argv);
  logVarOutOfScope("region", & region);
  logVarOutOfScope("region1", & region1);
  logVarOutOfScope("tmp", & tmp);
  {
  logComment("Function Exited");
  return (0);
  }
  }
}
}
